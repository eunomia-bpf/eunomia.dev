# AgentSight Website

Static product website for `agentsight.us`.

## Quick Start

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The static export is written to `out/` and is intended for Cloudflare Pages.
