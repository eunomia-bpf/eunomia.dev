{
  "name": "eunomia-site-audit",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:http": "node scripts/http-audit.mjs",
    "audit:parity": "node scripts/sitemap-parity.mjs",
    "audit:browser": "node scripts/browser-smoke.mjs",
    "audit:links": "node scripts/link-crawl.mjs",
    "audit:runtime": "node scripts/runtime-audit.mjs",
    "audit:rollout": "node scripts/rollout-audit.mjs",
    "audit": "npm run audit:http && npm run audit:parity && npm run audit:browser && npm run audit:links && npm run audit:runtime && npm run audit:rollout",
    "verify": "npm run audit"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "playwright": "^1.52.0"
  }
}
