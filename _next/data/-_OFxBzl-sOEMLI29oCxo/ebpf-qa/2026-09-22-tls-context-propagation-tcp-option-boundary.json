{"pageProps":{"eyebrow":"eBPF Q&A","page":{"layout":"document","title":"Why do TLS-encrypted service-to-service traces not stitch together in zero-code eBPF tracing, and which component actually bridges the encrypted hop?","description":"Short answer: because the trace context travels in the HTTP headers, and TLS encrypts exactly that payload.","descriptionIsExcerpt":true,"bodyHtml":"<p><strong>Short answer:</strong> because the trace context travels in the HTTP headers, and TLS encrypts exactly that payload. A zero-code eBPF tracer cannot decrypt in kernel space, so it can neither read nor inject <code>traceparent</code> inside ciphertext. The bridge is not the payload but the transport: OpenTelemetry eBPF Instrumentation (OBI) rides the context on a <strong>custom TCP option (kind 25) attached to the connection before TLS</strong>, which the kernel can see regardless of payload encryption, plus — for Go applications only — a userspace uprobe that writes the header <em>before</em> the TLS layer encrypts it. The cost is that this channel is OBI-specific (only other OBI-instrumented endpoints understand it) and connection-scoped, so it breaks at L7 proxies that discard and replay packets, and it cannot represent multiple concurrent streams on one HTTP/2 or gRPC connection.</p>\n<h2 id=\"why-encrypted-hops-break-header-based-propagation\">Why encrypted hops break header-based propagation</h2>\n<p>Zero-code distributed tracing works by serializing the W3C <code>traceparent</code> value across the boundary where the request crosses: the eBPF program reads the incoming context, carries it through the process, and writes it onto the outgoing request. In plaintext HTTP/1, the request bytes (headers included) flow through the kernel socket buffer in the clear. OBI's <code>kprobe</code> on <code>tcp_sendmsg</code>/<code>tcp_recvmsg</code> sees them, and the <code>sk_msg</code> program (tpinjector) can even extend the packet to add the header where the application did not. Any W3C-SDK peer will also understand it, because <code>traceparent</code> is a standard header.</p>\n<p>For TLS the situation inverts. The application's TLS library encrypts the request in user space before it ever reaches the kernel socket buffer. From the kernel's point of view the bytes are ciphertext:</p>\n<ul>\n<li>a <code>kprobe</code> on <code>tcp_sendmsg</code>/<code>tcp_recvmsg</code> cannot parse HTTP headers out of it;</li>\n<li>the <code>sk_msg</code> injector cannot splice a <code>traceparent</code> header into ciphertext;</li>\n<li>and the receiving side cannot extract context the sender's application encrypted.</li>\n</ul>\n<p>So header injection, the primary W3C-compatible channel, is simply unavailable on an encrypted hop. This is a property of where TLS terminates (user space, above the eBPF probes), not a configuration gap: enabling more probes does not let the kernel decrypt.</p>\n<h2 id=\"what-actually-carries-the-context-across-tls\">What actually carries the context across TLS</h2>\n<p>OBI's <a href=\"https://github.com/open-telemetry/opentelemetry-ebpf-instrumentation/blob/main/devdocs/context-propagation.md\">context propagation architecture</a> documents three injection methods, and each TLS case lands in a different one:</p>\n<ol>\n<li><strong>HTTP/1 headers (L7)</strong> — <code>traceparent</code> in plaintext HTTP. W3C-compatible, readable by any SDK-instrumented peer. Unavailable on TLS.</li>\n<li><strong>TCP options (L4)</strong> — a custom TCP option, kind 25, written onto the connection's segments. It exists <em>before</em> TLS (in the IP packet), so it is unaffected by payload encryption. The sender's tpinjector schedules the option; the receiver's <code>BPF_SOCK_OPS</code> program parses it into the <code>incoming_trace_map</code> on ingress. Two hard limits: the option is connection-scoped, and only OBI-instrumented endpoints know kind 25, so the context does not reach an SDK-only peer. The OBI docs state the consequence directly: for TLS, OBI \"injects the information at TCP/IP packet level\" and \"is only able to send the trace information to other OBI instrumented services\".</li>\n<li><strong>Per-stream HPACK injection (L7, multiplexed protocols)</strong> — for HTTP/2 and gRPC, one connection carries N concurrent streams, so a single connection-scoped TCP option cannot represent N distinct trace contexts (\"a connection-scoped option cannot represent N concurrent stream contexts\"). OBI instead writes a per-stream <code>traceparent</code> HPACK field into outgoing HEADERS frames via <code>bpf_msg_push_data</code>, which is the only network mechanism for multiplexed HTTP/2.</li>\n</ol>\n<p>The per-stream HPACK path meets TLS again: for generic (non-Go) TLS HTTP/2 the HPACK splice would land in ciphertext, so it cannot work — the documented boundary is that \"Generic non-gRPC HTTP/2 context propagation remains limited to Go library instrumentation.\" The Go exception works because a userspace uprobe into Go's HTTP/TLS layer writes the context into Go's plaintext request buffer <em>before</em> the TLS layer encrypts it (Go's <code>persistConnRoundTrip</code> path uses <code>bpf_probe_write_user</code> into the application buffer), so the encrypted wire carries a header the receiver's TLS layer will eventually expose.</p>\n<p>On ingress the layers use \"last one wins\": <code>BPF_SOCK_OPS</code> parses TCP options first, the <code>kprobe</code>/<code>protocol_http</code> layer parses HTTP headers later and overwrites, so the most reliable method (standard headers) naturally takes priority when both are present. Notably, receiving-side parsing of incoming <code>traceparent</code> from OpenTelemetry SDK-instrumented services \"still work[s]\" even when the outgoing TCP-option channel is disrupted — a peer that sends standard headers in the clear can always be read.</p>\n<p>The middlebox caveat is the operational detail that surprises people most. A custom TCP option on an established connection is not preserved end-to-end through many network paths: L7 proxies and load balancers discard the original packets and replay them on new connections, and some middleboxes or managed endpoints drop or reset the segment carrying the unknown option — the client then sees <code>connection reset by peer</code>, typically on the first request after connect, and the effect is path-dependent, so it appears intermittently. The documented remedy: where instrumented services talk through such intermediaries, use <code>headers</code> and do not enable <code>tcp</code>; TCP options are safe only when OBI is on both ends and the path preserves them (a direct L2/L3 network with no option-stripping middlebox).</p>\n<h2 id=\"how-to-verify-which-case-you-hit\">How to verify which case you hit</h2>\n<ol>\n<li><strong>Check the config state.</strong> Network-level context propagation in OBI is disabled by default. Enable it via <code>OTEL_EBPF_BPF_CONTEXT_PROPAGATION=all</code> (or a subset) or the <code>context_propagation</code> key in the OBI configuration (<code>all</code>, <code>headers</code>, <code>tcp</code>, or <code>headers,tcp</code>; the former <code>http</code> alias is removed and the deprecated <code>ip</code> value has no effect).</li>\n<li><strong>Read the wire.</strong> A <code>tcpdump</code> of the connection between two instrumented TLS endpoints should show the kind-25 TCP option on the sender's segments. If the option disappears after an L7 proxy or load balancer, the proxy is the boundary: the hop after it cannot carry L4 context, and the trace will split at that point.</li>\n<li><strong>Watch for the reset signature.</strong> Intermittent <code>connection reset by peer</code> on the first request after connect, on TLS paths with <code>tcp</code> enabled, is the option-stripping middlebox symptom described in the docs; dropping <code>tcp</code> from the propagation mode removes it.</li>\n<li><strong>Confirm the receiving side.</strong> OBI parses incoming <code>traceparent</code> automatically on both header and TCP-option channels, so an SDK-instrumented upstream sending standard headers will still stitch correctly even when the L4 channel is dead — check that the server span carries the upstream trace ID.</li>\n<li><strong>Check the protocol and runtime.</strong> For HTTP/2 or gRPC over TLS, the trace only stitches when the peer is Go-instrumented (uprobe-before-encryption); a generic-language TLS HTTP/2 client is at the documented limit, and its per-stream contexts cannot be injected by OBI over the encrypted hop.</li>\n</ol>\n<h2 id=\"where-the-answer-stops\">Where the answer stops</h2>\n<ul>\n<li>The kind-25 TCP option is OBI-specific. It is not a W3C mechanism: an SDK-only peer cannot read it, and the context is silently dropped at that boundary.</li>\n<li>L7 proxies and load balancers that terminate and re-establish TCP connections break the L4 channel; the trace then splits exactly at the proxy.</li>\n<li>Option-stripping middleboxes and managed endpoints can turn the channel into connection resets rather than clean non-propagation.</li>\n<li>Multiplexed TLS HTTP/2 and gRPC contexts are injectable only through Go's user-space uprobe path; other runtimes have no TLS-capable per-stream mechanism in the zero-code layer.</li>\n<li>None of this changes the receiving side's ability to read standard headers: incoming <code>traceparent</code> from any W3C-compliant sender (SDK or OBI) is parsed in the clear wherever the bytes are visible to the probes.</li>\n</ul>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://github.com/open-telemetry/opentelemetry-ebpf-instrumentation/blob/main/devdocs/context-propagation.md\">OBI context propagation architecture (devdoc)</a></li>\n<li><a href=\"https://github.com/open-telemetry/opentelemetry-ebpf-instrumentation/blob/main/devdocs/grpc-context-propagation.md\">OBI gRPC/HTTP2 context propagation (devdoc)</a></li>\n<li><a href=\"https://opentelemetry.io/docs/zero-code/obi/distributed-traces/\">OBI distributed traces documentation (TLS limitations and <code>context_propagation</code> config)</a></li>\n<li><a href=\"https://www.w3.org/TR/trace-context/\">W3C Trace Context specification</a></li>\n</ul>\n<h2 id=\"community-discussion-today\">Community discussion today</h2>\n<p>Honest coverage note: the two watchlist-opted Slack archives returned <strong>zero messages</strong> in the rolling 7-day window today, and the allowlisted Discord channels are visible-browser-only, with no visible browser session available in this run — so no private community material was available for 2026-09-22. The question above is the fallback selection: a genuine, recurring, still-open boundary of the monitored OpenTelemetry eBPF community, grounded entirely in the public primary sources above rather than in any thread.</p>\n<p>The recurring practitioner symptom in that space — \"my service-to-service traces stop at the TLS hop\" — resolves to exactly the boundary documented here: header-based propagation dies where TLS terminates, the L4 TCP-option channel is the only zero-code bridge for TLS, and that bridge has a hard compatibility surface (OBI-only peers, option-preserving paths, and per-stream limits on multiplexed protocols). Two themes from the public documentation recur in this space: (1) the intermittent first-request <code>connection reset by peer</code> that option-stripping middleboxes produce, which is easy to misread as application flakiness when <code>tcp</code> propagation is enabled behind an L7 proxy; and (2) the silent split at any non-OBI endpoint, where the TCP option is simply ignored rather than rejected, so traces look complete on one side and orphaned on the other. The open question the documentation itself leaves unresolved is what happens to the L4 channel on paths with stateful load balancers and NAT devices that rewrite segment headers; the documented workaround is to fall back to <code>headers</code> and accept that non-OBI or proxied peers break the chain.</p>","tags":[],"headings":[{"id":"why-encrypted-hops-break-header-based-propagation","text":"Why encrypted hops break header-based propagation","depth":2},{"id":"what-actually-carries-the-context-across-tls","text":"What actually carries the context across TLS","depth":2},{"id":"how-to-verify-which-case-you-hit","text":"How to verify which case you hit","depth":2},{"id":"where-the-answer-stops","text":"Where the answer stops","depth":2},{"id":"references","text":"References","depth":2},{"id":"community-discussion-today","text":"Community discussion today","depth":2}],"sourcePath":"https://github.com/eunomia-bpf/eunomia.dev/tree/main/docs/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary.md","metadata":{"updatedAt":"2026-09-22T23:36:34.000Z","createdAt":"2026-09-22T23:36:34.000Z","authors":[{"name":"Yunwei 123","email":"yunwei356@gmail.com"}]},"path":"/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary/","alternates":{"en":"/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary/","zh":"/zh/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary/"},"continuation":{"index":{"title":"eBPF Q&A","description":"Daily, source-grounded answers to recurring questions about eBPF, Linux observability, profiling, runtime extension, and security.","href":"/ebpf-qa/"},"previous":{"title":"For a Node.js service instrumented by OBI, why can't I just turn off the per-callback trace-context sentinel because the service doesn't use manual spans or log enrichment?","description":"Short answer: because the sentinel is not a feature of OBI's; it is the write side of a shared, pinned kernel map whose consumer set extends beyond OBI's own configuration.","href":"/ebpf-qa/2026-09-21-nodejs-traces-ctx-sentinel-gating-consumers/"},"next":{"title":"Does deleting a key from a BPF hash map zero out the value memory, and can a reused slot still hold the old value bytes?","description":"Short answer: No. bpfmapdeleteelem unlinks the element from the hash buckets and hands its memory back to the per-CPU freelist (preallocated hash) or to the RCU-deferred BPF allocator (a BPFFNOPREALLOC hash); it does","href":"/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete/"}},"sidebar":[{"title":"eBPF Q&A","items":[{"title":"Can eBPF detect secrets in network traffic without collecting them?","href":"/ebpf-qa/2026-08-08-detect-secrets-with-ebpf/","depth":1},{"title":"Why can an extensible BPF scheduler fail until pahole is upgraded?","href":"/ebpf-qa/2026-08-09-sched-ext-pahole-version/","depth":1},{"title":"Why can inserting a socket into SOCKHASH from TC egress soft-lock the kernel?","href":"/ebpf-qa/2026-08-10-tc-egress-sockhash-soft-lock/","depth":1},{"title":"Why can scxctl accept a scheduler switch while the service still does not start as intended?","href":"/ebpf-qa/2026-08-11-scxctl-scheduler-arguments/","depth":1},{"title":"How should OpenInference coexist with OpenTelemetry's GenAI semantic conventions?","href":"/ebpf-qa/2026-08-13-openinference-opentelemetry-genai/","depth":1},{"title":"How should a Linux-VM eBPF backend support macOS and Windows without mislabeling host coverage?","href":"/ebpf-qa/2026-08-15-cross-platform-ebpf-linux-vm-backend/","depth":1},{"title":"How should eBPF programs carry per-packet metadata across networking hooks?","href":"/ebpf-qa/2026-08-16-cross-hook-packet-metadata/","depth":1},{"title":"Why can classic uprobe BPF programs crash preemptible kernels that use private BPF stacks?","href":"/ebpf-qa/2026-08-17-uprobe-private-stack-preemption-crash/","depth":1},{"title":"Does cgroup v2 cpu.max still limit CPU time under BPF extensible scheduling?","href":"/ebpf-qa/2026-08-18-sched-ext-cgroup-cpu-max/","depth":1},{"title":"Why can libbpf load a BPF object slowly on a host with many kernel modules?","href":"/ebpf-qa/2026-08-19-libbpf-selective-kmod-btf-loading/","depth":1},{"title":"How can you tell whether an OpenTelemetry GenAI attribute is stable enough to depend on?","href":"/ebpf-qa/2026-08-20-opentelemetry-genai-attribute-stability/","depth":1},{"title":"Should an OpenTelemetry metrics producer copy service identity into every data point?","href":"/ebpf-qa/2026-08-21-opentelemetry-resource-attributes-prometheus-labels/","depth":1},{"title":"Why can an unused OBI Prometheus endpoint keep growing memory?","href":"/ebpf-qa/2026-08-22-obi-unused-prometheus-exporter-memory-growth/","depth":1},{"title":"Should an OpenTelemetry GenAI evaluation result carry a verifiable-evidence reference?","href":"/ebpf-qa/2026-08-23-opentelemetry-genai-evaluation-evidence-reference/","depth":1},{"title":"Why can a syscall-rewriting trampoline crash threads created by clone or clone3?","href":"/ebpf-qa/2026-08-24-clone-syscall-trampoline-child-stack/","depth":1},{"title":"How can you start a schedext scheduler at boot without systemd?","href":"/ebpf-qa/2026-08-25-sched-ext-boot-without-systemd/","depth":1},{"title":"Why can a single compare-and-swap lose atomic min/max updates under contention?","href":"/ebpf-qa/2026-08-26-atomic-min-max-cas-contention/","depth":1},{"title":"Can an unprivileged container create its own BPF token?","href":"/ebpf-qa/2026-08-28-unprivileged-container-bpf-token/","depth":1},{"title":"Why must releasing a BPF dynptr invalidate every derived slice and clone?","href":"/ebpf-qa/2026-08-29-bpf-dynptr-release-slice-clone-lifetime/","depth":1},{"title":"Why are PID and TID insufficient to correlate concurrent TLS, HTTP/2, and SSE traffic?","href":"/ebpf-qa/2026-08-31-tls-http2-sse-connection-correlation/","depth":1},{"title":"Why can't an eBPF program read bpftailcall()'s return value?","href":"/ebpf-qa/2026-09-02-bpf-tail-call-return-value/","depth":1},{"title":"Why can reverse-path filtering drop return traffic in an eBPF Kubernetes datapath?","href":"/ebpf-qa/2026-09-03-ebpf-kubernetes-rp-filter-return-traffic/","depth":1},{"title":"Why can the eBPF verifier lose a relationship that is true for the low 32 bits?","href":"/ebpf-qa/2026-09-04-ebpf-verifier-low-32-bit-scalar-equality/","depth":1},{"title":"Which failure scenarios are mandatory before a transient-TCP-recovery eBPF benchmark is meaningful?","href":"/ebpf-qa/2026-09-06-tcp-recovery-benchmark-failure-scenarios/","depth":1},{"title":"Which technical boundaries actually constrain malicious eBPF payloads in practice?","href":"/ebpf-qa/2026-09-08-malicious-ebpf-payload-boundaries/","depth":1},{"title":"What eBPF/kernel limitations should a process behavior reconstruction tool account for in its architecture?","href":"/ebpf-qa/2026-09-10-process-behavior-reconstruction-event-loss/","depth":1},{"title":"Why can't SIGKILL stop a BPF program load after verification has succeeded?","href":"/ebpf-qa/2026-09-11-bpf-post-verification-rewrite-sigkill/","depth":1},{"title":"When should I collect eBPF samples in a BPF ring buffer instead of a perf event array?","href":"/ebpf-qa/2026-09-12-bpf-ringbuffer-vs-perf-event-array/","depth":1},{"title":"Why does a kprobe on a function never fire when the compiler inlined it?","href":"/ebpf-qa/2026-09-13-kprobe-inlined-function-never-fires/","depth":1},{"title":"Why can't a BPF program sleep or block, and what should you use instead in a non-sleepable context?","href":"/ebpf-qa/2026-09-14-bpf-sleep-non-sleepable-context-alternatives/","depth":1},{"title":"Can a BPF program safely read user-space memory that belongs to another process?","href":"/ebpf-qa/2026-09-15-bpf-read-other-task-user-memory-zeros/","depth":1},{"title":"Why did enabling an eBPF tracer on a Node.js service triple p99 on the hot path when the eBPF probes are cheap?","href":"/ebpf-qa/2026-09-16-nodejs-tracer-async-hooks-p99-cost/","depth":1},{"title":"Should GenAI agent spans nest each tool execution under the model call that requested it, or keep model calls and tool executions as siblings under one agent span?","href":"/ebpf-qa/2026-09-17-genai-agent-invoke-agent-chat-execute-tool-sibling-tree/","depth":1},{"title":"Why does an injected Node.js agent from an eBPF tracer stay inside a process after the service is excluded, until a pod restart?","href":"/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart/","depth":1},{"title":"Why does the OBI config migration from v1 to v2 refuse to write a file when some fields have no mapping, and what is the middle ground?","href":"/ebpf-qa/2026-09-19-obi-config-migration-v1-v2-partial-fields/","depth":1},{"title":"How do you get observability out of a hosted agent harness when the agent loop runs in a vendor service rather than your process?","href":"/ebpf-qa/2026-09-20-hosted-agent-harness-otel-export-boundary/","depth":1},{"title":"For a Node.js service instrumented by OBI, why can't I just turn off the per-callback trace-context sentinel because the service doesn't use manual spans or log enrichment?","href":"/ebpf-qa/2026-09-21-nodejs-traces-ctx-sentinel-gating-consumers/","depth":1},{"title":"Why do TLS-encrypted service-to-service traces not stitch together in zero-code eBPF tracing, and which component actually bridges the encrypted hop?","href":"/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary/","depth":1},{"title":"Does deleting a key from a BPF hash map zero out the value memory, and can a reused slot still hold the old value bytes?","href":"/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete/","depth":1},{"title":"Why does a BPF LPM-trie lookup return a less specific stored prefix, and how does the key prefixlen field decide which entry wins?","href":"/ebpf-qa/2026-09-24-bpf-lpm-trie-lookup-prefixlen-caps-match/","depth":1},{"title":"Why does a full BPF hash map reject new keys with E2BIG, while updating an existing key still succeed?","href":"/ebpf-qa/2026-09-25-bpf-hash-map-full-update-vs-insert/","depth":1},{"title":"eBPF Q&A","href":"/ebpf-qa/","depth":0}]}]}},"__N_SSG":true}