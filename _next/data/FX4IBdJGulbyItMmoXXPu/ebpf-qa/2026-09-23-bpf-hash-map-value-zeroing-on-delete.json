{"pageProps":{"eyebrow":"eBPF Q&A","page":{"layout":"document","title":"Does deleting a key from a BPF hash map zero out the value memory, and can a reused slot still hold the old value bytes?","description":"Short answer: No. bpfmapdeleteelem unlinks the element from the hash buckets and hands its memory back to the per-CPU freelist (preallocated hash) or to the RCU-deferred BPF allocator (a BPFFNOPREALLOC hash); it does","descriptionIsExcerpt":true,"bodyHtml":"<p><strong>Short answer:</strong> No. <code>bpf_map_delete_elem</code> unlinks the element from the hash buckets and hands its memory back to the per-CPU freelist (preallocated hash) or to the RCU-deferred BPF allocator (a <code>BPF_F_NO_PREALLOC</code> hash); it does <strong>not</strong> clear the value bytes. You cannot read a deleted value back <em>through the map API</em> — <code>bpf_map_lookup_elem</code> returns <code>NULL</code> and <code>bpf_map_get_next_key</code> skips it — but the physical bytes stay in the reused slot until a later update overwrites them, and a normal full update overwrites the whole value region on every CPU — with one exception: on the <code>BPF_F_CPU</code> create path for per-CPU hash maps, only the named CPU's slot is written and the other CPUs keep the previous occupant's bytes, so a lookup of the new key (with <code>BPF_F_CPU</code> targeting a non-named CPU) can still read a deleted key's per-CPU values there. Until a zero-fill patch posted to the bpf mailing list on 2026-09-23 is merged (not merged as of the 2026-09-24 mainline fetch), that case is the <em>only</em> cross-key value leak reachable through the map API: on ordinary HASH maps a full update rewrites the whole value region on every CPU before the key becomes visible, and BPF-program creation of a per-CPU value goes through <code>pcpu_init_value</code>, which zeros the non-current CPU slots. The rule: never rely on <code>delete</code> (or LRU eviction) to erase value contents; zero the value yourself, and do not assume embedded kernel object fields (timers, workqueues, task work queues) survive, because the kernel cancels those fields when the element is freed.</p>\n<h2 id=\"what-delete-actually-does\">What delete actually does</h2>\n<p><code>htab_map_delete_elem</code> computes the bucket for the key, takes the bucket lock, and — if the element is present — calls <code>hlist_nulls_del_rcu(&#x26;l->hash_node)</code> to unlink it, then <code>free_htab_elem</code>. There is no <code>memset</code> or equivalent on the value region anywhere in that path. What happens to the memory next depends on how the map was created:</p>\n<ul>\n<li><strong>Preallocated hash (the default).</strong> At map creation, <code>prealloc_init</code> allocates all <code>max_entries</code> elements in one contiguous <code>htab->elems</code> pool and fills a per-CPU freelist. On delete the element goes back onto that per-CPU freelist (<code>pcpu_freelist_push</code>). It is immediately reusable — the next same-CPU update pops it back (<code>__pcpu_freelist_pop</code>, LIFO) — and the value bytes were never touched at delete time. The pool itself was zeroed once at map creation (<code>__GFP_ZERO</code> in <code>__bpf_map_area_alloc</code>, <code>kernel/bpf/syscall.c</code>); for per-CPU variants the separate per-CPU value area allocated by <code>bpf_map_alloc_percpu</code> inside <code>prealloc_init</code> is zeroed at creation by the kernel's <code>alloc_percpu</code> zero-fill guarantee (the BPF code passes no <code>__GFP_ZERO</code> flag — the zeroing is a property of <code>alloc_percpu</code> itself). Neither area is re-zeroed on delete; on element <em>reuse</em>, the only post-creation zeroing anywhere is <code>pcpu_init_value</code> zeroing the non-current CPU slots when a BPF program creates a per-CPU value.</li>\n<li><strong>Lazy hash (<code>BPF_F_NO_PREALLOC</code>).</strong> Elements are allocated on demand from the per-CPU BPF memory cache, and on delete <code>htab_elem_free</code> returns them to that allocator. The physical free is <strong>RCU-deferred</strong> (the allocator's <code>free_by_rcu</code> / <code>call_rcu_tasks_trace</code> path), so the page is not recycled until a grace period completes; the value bytes persist even longer.</li>\n</ul>\n<p>The element <em>count</em> is decremented synchronously in both paths (<code>bpf_map_dec_elem_count</code> / <code>dec_elem_count</code>), which is why a delete immediately makes room for a new element even though the underlying <em>memory</em> may be recycled a little later.</p>\n<h2 id=\"why-a-full-update-cannot-leak-one-keys-value-into-another\">Why a full update cannot leak one key's value into another</h2>\n<p>When a new key is inserted into a recycled element, <code>alloc_htab_elem</code> copies the full <code>key_size</code> bytes and then copies the <strong>entire</strong> value region (<code>copy_map_value</code>, or a rounded-<code>value_size</code> <code>memcpy</code>). A well-formed, fully-written value therefore completely replaces the previous occupant's bytes. There is no path where you <code>bpf_map_lookup_elem</code> key B and read key A's value through the map API — with one documented exception: on the <code>BPF_F_CPU</code> create path for per-CPU maps, <code>pcpu_copy_value</code> writes only the named CPU's slot and returns early, leaving the other CPUs with the recycled element's previous bytes, so a lookup of the new key returns a deleted key's per-CPU values on those CPUs. A zero-fill patch for that arm (<code>Fixes: c6936161fd55</code>) was posted to the bpf mailing list on 2026-09-23 and is not merged as of the 2026-09-24 mainline fetch. The residual from an ordinary full update or a BPF-program write is a <em>physical-memory</em> fact (the old bytes sit in the slot until overwritten or scrubbed), not a <em>logical</em> leak: on ordinary HASH maps the value region is fully rewritten on every CPU before the key is visible, and BPF-program creation of a per-CPU value zeros the non-current CPU slots via <code>pcpu_init_value</code>. The <code>BPF_F_CPU</code> create-path exception above is the one <em>logical</em>, API-visible leak: until the patch lands, <code>bpf_map_lookup_elem_flags</code> with <code>BPF_F_CPU|cpu&#x3C;&#x3C;32</code> targeting a non-named CPU returns the previous occupant's bytes.</p>\n<h2 id=\"the-corners-that-are-not-safe\">The corners that are not safe</h2>\n<ul>\n<li><strong>The value bytes are not erased.</strong> Delete contains no scrub step, and the lazy allocator recycles its objects without a scrub a BPF program can rely on. If a value ever held secret data, <code>delete</code> does not wipe it — the bytes linger in the preallocated pool or the allocator's freelist until reuse. For an explicit <code>delete</code>, the safe pattern — and the only reliable erasure — is to zero the value explicitly before releasing the entry; automatic LRU eviction gives no user-space hook before the element leaves, so an evicted value's bytes may survive in the pool until a later allocation overwrites them. (Creation-time zeroing — <code>__GFP_ZERO</code> for the non-per-CPU pool, the <code>alloc_percpu</code> guarantee for per-CPU value areas — is a one-time event; it does not apply on delete or reuse.)</li>\n<li><strong>Embedded kernel object fields are cancelled, not preserved.</strong> On free, <code>check_and_cancel_fields</code> / <code>bpf_obj_cancel_fields</code> cancel the value's BPF object fields (timers, workqueues, task work queues). For per-CPU hash maps these field types are rejected at map creation by <code>map_check_btf()</code>, so the cancellation is a no-op there; for non-per-CPU maps it is a real guarantee. A program that reads the value after the element is freed or recycled cannot assume an embedded object is still valid.</li>\n<li><strong>LRU maps evict on their own.</strong> <code>BPF_MAP_TYPE_LRU_HASH</code> (and <code>LRU_PERCPU_HASH</code>) evict the least-recently-used element when the map is full, through the same unlink-and-free path (<code>htab_lru_map_delete_node</code>: <code>hlist_nulls_del_rcu</code> + element-count decrement + cancel fields, no zeroing). A key you never called <code>delete</code> on can read back <code>NULL</code> after eviction. With <code>BPF_F_NO_COMMON_LRU</code> each CPU keeps its own LRU list (per-CPU LRU + per-CPU map for <code>LRU_PERCPU_HASH</code>; per-CPU LRU + global map for <code>LRU_HASH</code>), so which element evicts is per-CPU rather than global.</li>\n</ul>\n<h2 id=\"how-to-verify-it\">How to verify it</h2>\n<ol>\n<li><strong>Confirm the unlink.</strong> <code>bpf_map_lookup_elem(key)</code> returns <code>NULL</code> right after <code>bpf_map_delete_elem(key)</code>, and <code>bpf_map_get_next_key</code> / <code>bpf_map_get_next_key_batch</code> never return the deleted key.</li>\n<li><strong>Mind the iteration restart.</strong> The docs note that if you pass a <code>cur_key</code> that has just been deleted, <code>bpf_map_get_next_key</code> restarts from the <em>first</em> key in the table; use the batched lookup for a stable sweep.</li>\n<li><strong>Contrast with arrays.</strong> <code>BPF_MAP_TYPE_ARRAY</code> values are <em>zero-initialized at creation time</em> (documented). Non-per-CPU preallocated hash values are likewise zeroed at map creation (<code>__GFP_ZERO</code> in <code>__bpf_map_area_alloc</code>); per-CPU value areas are zeroed at creation by the <code>alloc_percpu</code> guarantee. What differs: arrays are not recycled element-by-element, whereas hash elements are — and no path re-zeros a hash value on delete (the only post-creation zeroing is <code>pcpu_init_value</code> zeroing the non-current CPU slots when a BPF program creates a per-CPU value). That recycling-without-rezeroing is the whole boundary.</li>\n<li><strong>Guarantee clean values.</strong> For an explicit <code>delete</code>, the only reliable erasure is to write zeros before <code>delete</code>; automatic LRU eviction has no guaranteed user-space point before the element leaves, so scrub-on-eviction cannot be guaranteed; there is no scrub flag on <code>bpf_map_delete_elem</code>.</li>\n</ol>\n<h2 id=\"where-the-answer-stops\">Where the answer stops</h2>\n<ul>\n<li>This is about value <em>memory</em>, not the map API's contract: a deleted or evicted key still reads as <code>NULL</code> and is not enumerated, so the <em>logical</em> view is clean even when the <em>physical</em> bytes are not — with the one exception above: on the unfixed <code>BPF_F_CPU</code> create path, a lookup of the new key on a non-named CPU is an API-visible cross-key <em>logical</em> leak until the zero-fill patch lands.</li>\n<li>Whether the slab later scrubs a recycled page is not a BPF-visible guarantee; a program must not depend on it.</li>\n<li>The per-CPU LRU flag (<code>BPF_F_NO_COMMON_LRU</code>) gives each CPU its own LRU list (per-CPU LRU + per-CPU map for <code>LRU_PERCPU_HASH</code>; per-CPU LRU + global map for <code>LRU_HASH</code>), changing <em>which</em> element evicts per CPU, but not the unlink-and-free-without-zeroing mechanism.</li>\n</ul>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://docs.kernel.org/bpf/map_hash.html\">BPF_MAP_TYPE_HASH, with PERCPU and LRU Variants (kernel docs)</a> — default preallocation, <code>BPF_F_NO_PREALLOC</code>, delete semantics, the <code>bpf_map_get_next_key</code> restart caveat, per-CPU slots.</li>\n<li><a href=\"https://docs.kernel.org/bpf/map_array.html\">BPF_MAP_TYPE_ARRAY (kernel docs)</a> — \"all array elements are pre-allocated and zero initialized when created.\"</li>\n<li><a href=\"https://raw.githubusercontent.com/torvalds/linux/master/kernel/bpf/hashtab.c\">kernel/bpf/hashtab.c</a> — <code>htab_map_delete_elem</code> → <code>free_htab_elem</code> → <code>pcpu_freelist_push</code> / <code>htab_elem_free</code>; <code>prealloc_init</code>; the full-value overwrite in <code>alloc_htab_elem</code>; <code>pcpu_init_value</code> (per-CPU create-path zeroing); <code>check_and_cancel_fields</code>.</li>\n<li><a href=\"https://raw.githubusercontent.com/torvalds/linux/master/kernel/bpf/memalloc.c\">kernel/bpf/memalloc.c</a> — the lazy BPF memory cache and its RCU-deferred free path.</li>\n<li><a href=\"https://raw.githubusercontent.com/torvalds/linux/master/kernel/bpf/syscall.c\">kernel/bpf/syscall.c</a> — <code>__GFP_ZERO</code> in <code>__bpf_map_area_alloc</code> (the prealloc element pool, line 384); <code>bpf_map_alloc_percpu</code> → <code>__alloc_percpu_gfp</code> (the per-CPU value area, line 583); <code>__bpf_alloc_page</code> (line 606); <code>bpf_map_value_size</code> returning the single value size under <code>BPF_F_CPU</code>/<code>BPF_F_ALL_CPUS</code> (line 137).</li>\n<li><a href=\"https://lists.openwall.net/linux-kernel/2026/09/23/16\">bpf mailing list, 2026-09-23: zero-fill patch for the <code>BPF_F_CPU</code> per-CPU hash create path</a> — the <code>pcpu_init_value</code> diff (<code>Fixes: c6936161fd55</code>); the element-reuse selftest is at <a href=\"https://lists.openwall.net/linux-kernel/2026/09/23/18\">message 18</a>. Not merged as of the 2026-09-24 mainline fetch. Cited by URL and subject only.</li>\n</ul>\n<h2 id=\"community-discussion-today\">Community discussion today</h2>\n<p>Honest coverage note: the two watchlist-opted Slack archives returned <strong>zero messages</strong> in the rolling window today, and the allowlisted Discord channels plus the public bpf mailing list and r/eBPF remain visible-browser-only, with no visible-browser session available in this run — so no private community material was available for 2026-09-23. The question above is the fallback selection: a genuine, recurring practitioner boundary of the monitored eBPF development community, grounded entirely in the public primary sources above (kernel BPF docs and upstream source) rather than in any thread.</p>\n<p>The recurring practitioner theme in that space — \"my hash-map values go stale, a deleted key still seems to show old data\" — resolves to exactly this boundary: deletion is an <em>unlink</em>, not a <em>zeroing</em>, and the element's memory is recycled through a per-CPU freelist (prealloc) or an RCU-deferred allocator (lazy). Two public-doc-anchored follow-ups recur here. First, the <strong>LRU</strong> surprise: a key that was never deleted can read back <code>NULL</code> once the map is full and the least-recently-used element evicts, which people misread as a delete bug when it is actually capacity eviction. Second, the <strong>secret-handling</strong> implication: because delete does not scrub the value bytes, a map that once held sensitive data still holds them physically until reuse, so the safe pattern is to zero the value (and drop embedded kernel objects explicitly) before releasing the entry. The boundary the docs leave open is that there is no scrub-on-delete API; keeping value memory clean is a program responsibility, not a kernel one. One public primary-source signal from the same day corroborates that this boundary is live in upstream development: the bpf mailing-list zero-fill patch for the <code>BPF_F_CPU</code> per-CPU create path described above (<a href=\"https://lists.openwall.net/linux-kernel/2026/09/23/16\">the patch</a>, with the element-reuse selftest at <a href=\"https://lists.openwall.net/linux-kernel/2026/09/23/18\">message 18</a>) — a same-day public signal, not a private-community message; the honest coverage note above is unchanged.</p>","tags":[],"headings":[{"id":"what-delete-actually-does","text":"What delete actually does","depth":2},{"id":"why-a-full-update-cannot-leak-one-keys-value-into-another","text":"Why a full update cannot leak one key's value into another","depth":2},{"id":"the-corners-that-are-not-safe","text":"The corners that are not safe","depth":2},{"id":"how-to-verify-it","text":"How to verify it","depth":2},{"id":"where-the-answer-stops","text":"Where the answer stops","depth":2},{"id":"references","text":"References","depth":2},{"id":"community-discussion-today","text":"Community discussion today","depth":2}],"sourcePath":"https://github.com/eunomia-bpf/eunomia.dev/tree/main/docs/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete.md","metadata":{"updatedAt":"2026-09-24T13:42:32.000Z","createdAt":"2026-09-24T01:42:43.000Z","authors":[{"name":"Yunwei","email":"yunwei356@gmail.com"},{"name":"Yunwei 123","email":"yunwei356@gmail.com"}]},"path":"/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete/","alternates":{"en":"/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete/","zh":"/zh/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete/"},"continuation":{"index":{"title":"eBPF Q&A","description":"Daily, source-grounded answers to recurring questions about eBPF, Linux observability, profiling, runtime extension, and security.","href":"/ebpf-qa/"},"previous":{"title":"Why do TLS-encrypted service-to-service traces not stitch together in zero-code eBPF tracing, and which component actually bridges the encrypted hop?","description":"Short answer: because the trace context travels in the HTTP headers, and TLS encrypts exactly that payload.","href":"/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary/"},"next":{"title":"eBPF Q&A","description":"Daily, source-grounded answers to recurring questions about eBPF, Linux observability, profiling, runtime extension, and security.","href":"/ebpf-qa/"}},"sidebar":[{"title":"eBPF Q&A","items":[{"title":"Can eBPF detect secrets in network traffic without collecting them?","href":"/ebpf-qa/2026-08-08-detect-secrets-with-ebpf/","depth":1},{"title":"Why can an extensible BPF scheduler fail until pahole is upgraded?","href":"/ebpf-qa/2026-08-09-sched-ext-pahole-version/","depth":1},{"title":"Why can inserting a socket into SOCKHASH from TC egress soft-lock the kernel?","href":"/ebpf-qa/2026-08-10-tc-egress-sockhash-soft-lock/","depth":1},{"title":"Why can scxctl accept a scheduler switch while the service still does not start as intended?","href":"/ebpf-qa/2026-08-11-scxctl-scheduler-arguments/","depth":1},{"title":"How should OpenInference coexist with OpenTelemetry's GenAI semantic conventions?","href":"/ebpf-qa/2026-08-13-openinference-opentelemetry-genai/","depth":1},{"title":"How should a Linux-VM eBPF backend support macOS and Windows without mislabeling host coverage?","href":"/ebpf-qa/2026-08-15-cross-platform-ebpf-linux-vm-backend/","depth":1},{"title":"How should eBPF programs carry per-packet metadata across networking hooks?","href":"/ebpf-qa/2026-08-16-cross-hook-packet-metadata/","depth":1},{"title":"Why can classic uprobe BPF programs crash preemptible kernels that use private BPF stacks?","href":"/ebpf-qa/2026-08-17-uprobe-private-stack-preemption-crash/","depth":1},{"title":"Does cgroup v2 cpu.max still limit CPU time under BPF extensible scheduling?","href":"/ebpf-qa/2026-08-18-sched-ext-cgroup-cpu-max/","depth":1},{"title":"Why can libbpf load a BPF object slowly on a host with many kernel modules?","href":"/ebpf-qa/2026-08-19-libbpf-selective-kmod-btf-loading/","depth":1},{"title":"How can you tell whether an OpenTelemetry GenAI attribute is stable enough to depend on?","href":"/ebpf-qa/2026-08-20-opentelemetry-genai-attribute-stability/","depth":1},{"title":"Should an OpenTelemetry metrics producer copy service identity into every data point?","href":"/ebpf-qa/2026-08-21-opentelemetry-resource-attributes-prometheus-labels/","depth":1},{"title":"Why can an unused OBI Prometheus endpoint keep growing memory?","href":"/ebpf-qa/2026-08-22-obi-unused-prometheus-exporter-memory-growth/","depth":1},{"title":"Should an OpenTelemetry GenAI evaluation result carry a verifiable-evidence reference?","href":"/ebpf-qa/2026-08-23-opentelemetry-genai-evaluation-evidence-reference/","depth":1},{"title":"Why can a syscall-rewriting trampoline crash threads created by clone or clone3?","href":"/ebpf-qa/2026-08-24-clone-syscall-trampoline-child-stack/","depth":1},{"title":"How can you start a schedext scheduler at boot without systemd?","href":"/ebpf-qa/2026-08-25-sched-ext-boot-without-systemd/","depth":1},{"title":"Why can a single compare-and-swap lose atomic min/max updates under contention?","href":"/ebpf-qa/2026-08-26-atomic-min-max-cas-contention/","depth":1},{"title":"Can an unprivileged container create its own BPF token?","href":"/ebpf-qa/2026-08-28-unprivileged-container-bpf-token/","depth":1},{"title":"Why must releasing a BPF dynptr invalidate every derived slice and clone?","href":"/ebpf-qa/2026-08-29-bpf-dynptr-release-slice-clone-lifetime/","depth":1},{"title":"Why are PID and TID insufficient to correlate concurrent TLS, HTTP/2, and SSE traffic?","href":"/ebpf-qa/2026-08-31-tls-http2-sse-connection-correlation/","depth":1},{"title":"Why can't an eBPF program read bpftailcall()'s return value?","href":"/ebpf-qa/2026-09-02-bpf-tail-call-return-value/","depth":1},{"title":"Why can reverse-path filtering drop return traffic in an eBPF Kubernetes datapath?","href":"/ebpf-qa/2026-09-03-ebpf-kubernetes-rp-filter-return-traffic/","depth":1},{"title":"Why can the eBPF verifier lose a relationship that is true for the low 32 bits?","href":"/ebpf-qa/2026-09-04-ebpf-verifier-low-32-bit-scalar-equality/","depth":1},{"title":"Which failure scenarios are mandatory before a transient-TCP-recovery eBPF benchmark is meaningful?","href":"/ebpf-qa/2026-09-06-tcp-recovery-benchmark-failure-scenarios/","depth":1},{"title":"Which technical boundaries actually constrain malicious eBPF payloads in practice?","href":"/ebpf-qa/2026-09-08-malicious-ebpf-payload-boundaries/","depth":1},{"title":"What eBPF/kernel limitations should a process behavior reconstruction tool account for in its architecture?","href":"/ebpf-qa/2026-09-10-process-behavior-reconstruction-event-loss/","depth":1},{"title":"Why can't SIGKILL stop a BPF program load after verification has succeeded?","href":"/ebpf-qa/2026-09-11-bpf-post-verification-rewrite-sigkill/","depth":1},{"title":"When should I collect eBPF samples in a BPF ring buffer instead of a perf event array?","href":"/ebpf-qa/2026-09-12-bpf-ringbuffer-vs-perf-event-array/","depth":1},{"title":"Why does a kprobe on a function never fire when the compiler inlined it?","href":"/ebpf-qa/2026-09-13-kprobe-inlined-function-never-fires/","depth":1},{"title":"Why can't a BPF program sleep or block, and what should you use instead in a non-sleepable context?","href":"/ebpf-qa/2026-09-14-bpf-sleep-non-sleepable-context-alternatives/","depth":1},{"title":"Can a BPF program safely read user-space memory that belongs to another process?","href":"/ebpf-qa/2026-09-15-bpf-read-other-task-user-memory-zeros/","depth":1},{"title":"Why did enabling an eBPF tracer on a Node.js service triple p99 on the hot path when the eBPF probes are cheap?","href":"/ebpf-qa/2026-09-16-nodejs-tracer-async-hooks-p99-cost/","depth":1},{"title":"Should GenAI agent spans nest each tool execution under the model call that requested it, or keep model calls and tool executions as siblings under one agent span?","href":"/ebpf-qa/2026-09-17-genai-agent-invoke-agent-chat-execute-tool-sibling-tree/","depth":1},{"title":"Why does an injected Node.js agent from an eBPF tracer stay inside a process after the service is excluded, until a pod restart?","href":"/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart/","depth":1},{"title":"Why does the OBI config migration from v1 to v2 refuse to write a file when some fields have no mapping, and what is the middle ground?","href":"/ebpf-qa/2026-09-19-obi-config-migration-v1-v2-partial-fields/","depth":1},{"title":"How do you get observability out of a hosted agent harness when the agent loop runs in a vendor service rather than your process?","href":"/ebpf-qa/2026-09-20-hosted-agent-harness-otel-export-boundary/","depth":1},{"title":"For a Node.js service instrumented by OBI, why can't I just turn off the per-callback trace-context sentinel because the service doesn't use manual spans or log enrichment?","href":"/ebpf-qa/2026-09-21-nodejs-traces-ctx-sentinel-gating-consumers/","depth":1},{"title":"Why do TLS-encrypted service-to-service traces not stitch together in zero-code eBPF tracing, and which component actually bridges the encrypted hop?","href":"/ebpf-qa/2026-09-22-tls-context-propagation-tcp-option-boundary/","depth":1},{"title":"Does deleting a key from a BPF hash map zero out the value memory, and can a reused slot still hold the old value bytes?","href":"/ebpf-qa/2026-09-23-bpf-hash-map-value-zeroing-on-delete/","depth":1},{"title":"eBPF Q&A","href":"/ebpf-qa/","depth":0}]}]}},"__N_SSG":true}