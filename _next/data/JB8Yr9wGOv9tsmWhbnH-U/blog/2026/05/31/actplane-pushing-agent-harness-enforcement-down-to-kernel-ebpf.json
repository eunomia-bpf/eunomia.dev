{"pageProps":{"eyebrow":"Blog","page":{"layout":"document","title":"ActPlane: Pushing Agent Harness Enforcement Down to Kernel eBPF","description":"ActPlane is an eBPF-based policy engine that observes and enforces AI agent behavior at the OS kernel level. This post analyzes the systemic blind spots of prompt constraints, tool-layer guards, and sandboxes, and explains how ActPlane uses label propagation and temporal predicates to implement a deterministic agent harness.","descriptionIsExcerpt":false,"bodyHtml":"<p>You wrote a rule in CLAUDE.md: \"do not run <code>git push</code>.\" The agent obeyed, and it never called the git tool. But it wrote a Python script that called <code>subprocess.run([\"git\", \"push\"])</code>, and the code was pushed to the remote. The prompt constraint was never violated.</p>\n<p>This scenario exposes a structural problem: <strong>agent constraints and agent side effects live on different planes.</strong> Prompt constraints operate at the reasoning layer, tool guards at the API layer, sandboxes at the container layer, but every side effect an agent produces ultimately passes through the OS kernel. Every exec, every file open, every network connect: no matter what path the agent takes, the kernel is there. If constraints aren't enforced at this layer, the agent can always find a way around them, and it doesn't even need to be malicious; it's just trying to finish the task you gave it.</p>\n<p><a href=\"https://github.com/eunomia-bpf/ActPlane\">ActPlane</a> is built on this insight. It installs a policy engine in the kernel via eBPF, observing and enforcing agent constraints at the syscall layer. When a rule matches, it fires with no dependency on the agent \"remembering\" anything. But ActPlane does more than block: when a constraint triggers, the agent receives human-readable feedback explaining why it was stopped and what to do instead. The agent understands the reason, takes a different path, and continues the task. This is the fundamental difference between a harness and a sandbox: a sandbox gives you a wall and a <code>Permission denied</code>; a harness gives you a rule and an alternative.</p>\n<h2 id=\"what-we-actually-want\">What We Actually Want</h2>\n<p>Before dissecting the problem, consider a few constraints that agents genuinely need in day-to-day work. None of them can be said clearly enough in a prompt, enforced by an MCP gateway, or expressed by a container sandbox.</p>\n<p>Preventing an agent from running <code>git push</code> sounds like an ordinary sandbox rule, until you realize that the coverage of \"prevent\" depends entirely on the layer doing the checking. The agent might call git directly, write a shell script that calls it, or generate a Python program with nested subprocess calls. The tool API layer can only check \"did the agent call the git tool,\" but the real question is \"did any process in this agent's process tree execute the git binary,\" and those two questions have completely different coverage.</p>\n<p>Now consider a temporal constraint: after modifying <code>specs/*</code>, the agent must run <code>protoc</code> before committing, otherwise it should get a reminder. We don't want to block the agent from editing spec files, since that's part of its normal work, but if it edits a spec and goes straight to commit, it skipped a step. The goal is not to prevent the commit, but to remind the agent before it happens so it can decide whether to run code generation.</p>\n<p>\"Tests must pass before committing\" follows similar logic but adds dynamic invalidation: every time the agent modifies a file under <code>src/</code>, previous test results should automatically expire. The check isn't \"have tests ever been run,\" but \"have tests been run since the last source file modification.\" Run tests, then change one line of code? The status resets and you must run them again.</p>\n<p>Finally, a mandatory mediation constraint: the production database <code>prod.db</code> may only be accessed through the migration tool, and the agent can't open it directly. No matter how the agent reaches the file-open call, if its process ancestor chain hasn't executed the <code>migrate</code> tool, the operation is blocked. What matters isn't whether the agent has some abstract permission, but which path it took to get here: pass through the designated gate program and you're allowed in; bypass it and you're stopped.</p>\n<p>Together, these four constraints involve process lineage tracking, temporal ordering, dynamic invalidation, and mandatory mediation, all beyond the scope of static allow/deny. To see why meeting them requires a kernel-level approach, let's look at where the existing three layers of constraints each fall short.</p>\n<h2 id=\"three-layers-three-blind-spots\">Three Layers, Three Blind Spots</h2>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Approach</th><th>What it does</th><th>What it can't cover</th></tr></thead><tbody><tr><td><strong>Prompt constraints</strong> (CLAUDE.md, AGENTS.md)</td><td>Tell the agent what to do and not do</td><td>Probabilistic: agents forget or non-maliciously route around them in long contexts</td></tr><tr><td><strong>Tool-layer guards</strong> (MCP gateway, AgentSpec)</td><td>Intercept and authorize at the tool API</td><td>Completely bypassed when agents shell out, link SDKs, or spawn subprocesses</td></tr><tr><td><strong>Sandboxes</strong> (containers, VMs, E2B, Daytona)</td><td>Isolate the entire execution environment</td><td>All-or-nothing: can't express \"file A may only be accessed via script A\" or \"run tests before committing\"</td></tr></tbody></table>\n<p>CLAUDE.md, AGENTS.md, and system prompts are the most common constraint mechanism today: natural-language instructions that depend on the model \"remembering and obeying\" during inference. The trouble is with \"remembering.\" When conversations exceed tens of thousands of tokens, early constraints get diluted in attention allocation by subsequent user instructions and tool outputs. The model hasn't technically forgotten the rule, but the rule's weight in decision-making drops as the context expands. In our <a href=\"/blog/2026/02/17/agentcgroup-what-happens-when-ai-coding-agents-meet-os-resources/\">AgentCgroup characterization study</a>, we observed that agents average dozens of tool calls per task and the conversation context grows very large. The earlier a constraint is set and the longer the conversation, the lower the probability of effective compliance. Even harder to defend against is indirect violation: the agent is told \"don't delete files,\" so it writes a Makefile target containing <code>rm -rf</code> and calls <code>make clean</code>. From the agent's perspective it just executed a build command, and each decision point is locally reasonable, but the resulting behavior chain violates the original policy. Prompt constraints govern what the agent intends; actual behavior is outside their jurisdiction.</p>\n<p>What if we push down one layer and intercept at the tool call API? MCP gateways, <a href=\"https://arxiv.org/abs/2503.18666\">AgentSpec</a>, and <a href=\"https://arxiv.org/abs/2504.11703\">Progent</a> are indeed far more reliable than prompts: if the agent calls <code>file_write(\"/etc/passwd\", ...)</code>, the tool layer rejects it outright, checking the operation itself rather than intent. But nearly every agent framework provides a shell tool, because agents need to run builds, execute tests, and call third-party CLIs. Once the agent has bash, it can write a Python script whose internals call <code>subprocess.run([\"curl\", \"-X\", \"POST\", ...])</code> to send data to an external network. The tool layer sees <code>python script.py</code> and knows nothing about the subprocesses spawned inside; the call chain can be three or four layers deep, but the tool layer only sees the first. Less obviously, agent-generated code can directly link database drivers or HTTP client libraries, producing side effects at runtime that never pass through any tool call path. The tool layer's vision stops at the registered tool entry point, and side effects from code execution are entirely outside its view.</p>\n<p>Pushing down one more layer, we reach full environment isolation. Containers, VMs, <a href=\"https://github.com/e2b-dev/E2B\">E2B</a>, and <a href=\"https://github.com/daytonaio/daytona\">Daytona</a> are the most reliable security boundary available today. For preventing agent escape to the host, they are the right answer. But the constraints agents actually need are far richer than \"can or can't access a resource.\" \"After modifying proto files, the agent must run protoc before committing\" is a temporal constraint, and sandboxes have no concept of time and only know which resources are accessible at the current instant. \"Sensitive data read from the database must not be written to logs\" requires tracking data flow, but sandboxes operate at process granularity and don't know what a process read or wrote internally. And the same <code>git commit</code> should have different policies depending on whether tests just ran or haven't, a distinction sandboxes can't make because they have no sense of historical context.</p>\n<p>A frequently underestimated problem compounds all of this: feedback quality. When a sandbox rejects an operation, the agent receives only <code>Permission denied</code> or <code>EPERM</code> and doesn't know why it was rejected or what to do to satisfy the constraint. The typical result is the agent retrying three to five times and then giving up on the entire task, or worse, trying another path to bypass the rejection and introducing new problems. Sandboxes answer \"can this process access this resource,\" but agents need far richer questions answered: under what conditions, in what order, and based on what data-flow history is this operation permitted?</p>\n<h2 id=\"the-path-all-side-effects-must-travel\">The Path All Side Effects Must Travel</h2>\n<p>All three layers' blind spots point in the same direction: the constraint mechanism must sit on the path that every operation travels through. <a href=\"https://arxiv.org/abs/2508.02736\">AgentSight</a> (2025) used eBPF to capture both intent-layer and action-layer agent data, coining the concept of a \"semantic gap\" between the two. ActPlane builds enforcement on that observability foundation. No matter what tool the agent uses, what script it writes, or how many layers of subprocesses it spawns, all side effects ultimately pass through OS kernel syscalls. ActPlane installs lightweight eBPF programs in the kernel, hooking the process lifecycle (<code>sched_process_fork</code>, <code>sched_process_exec</code>, <code>sched_process_exit</code>), file operations (<code>sys_enter_openat</code>, <code>sys_enter_unlinkat</code>, <code>sys_enter_renameat2</code>), and network connections (<code>sys_enter_connect</code>), then makes decisions based on labeled information-flow policies. Regardless of what path an agent takes, it will pass through these syscalls.</p>\n<p>Return to the opening scenario to see the difference: the agent wrote a Python script that spawned a subprocess calling git push. The tool layer only saw <code>python script.py</code>, but ActPlane sees all syscalls across the entire process tree, including that git call three layers deep. Because constraints attach directly to the process tree, \"Codex's entire subprocess tree cannot touch git\" is a single rule, with no need to guard every possible tool entry point.</p>\n<p>ActPlane goes beyond simply pushing tool-layer guards down to the kernel, introducing two capabilities that neither tool layers nor sandboxes have: <strong>data-flow tracking</strong> and <strong>temporal reasoning</strong>. Labels propagate across fork/exec and file read/write boundaries, making \"data read from A must not flow to B\" an expressible policy. The <code>since</code> clause lets rules dynamically update on the event timeline, turning \"have tests been run since the last source modification\" into a predicate that invalidates and rebuilds as new events occur. The next two sections unpack these mechanisms, but first let's clarify what ActPlane is, because it's not just a deeper sandbox.</p>\n<h2 id=\"harness-not-just-a-sandbox\">Harness, Not Just a Sandbox</h2>\n<p>A sandbox draws an isolation boundary: everything inside is allowed, everything outside is forbidden. For untrusted code this is the right model: you don't trust it, so you lock it in a cage. But an agent isn't untrusted code; it's your collaborator, and you want it to complete the task while following certain constraints along the way.</p>\n<p>Many of these constraints have nothing to do with security permissions, yet they're exactly the kind of rules agents need when operating autonomously in real codebases. \"Run tests before committing\" is an engineering workflow, \"access prod.db through the migration tool\" is an operational standard, \"don't mix independent tasks in one commit\" is a work habit. Sandboxes can't express them because their semantics go beyond resource access. A harness, however, also subsumes sandbox capabilities: when the agent runs an untrusted command, you can write a rule confining the entire subtree to read-only, no-network, or a specific directory. In ActPlane this is just a subset of the rules, coexisting with workflow rules in the same policy file.</p>\n<p>The feedback loop is the most critical part of harness design. When a rule fires, ActPlane delivers the reason to the agent through its framework's hook system:</p>\n<pre><code class=\"\">🚫 KILLED: process 'git' (pid 4213, ppid 4210) — /usr/bin/git\n   effect: kill\n   reason: no git under the agent; use the review workflow\n</code></pre>\n<p>The agent reads the reason, understands the constraint, and takes a different path to complete the task. It doesn't need to \"remember\" that it can't use git beforehand; it can try, get told why it can't and what to do instead, and self-correct. This produces an interesting architectural pattern: the agent's reasoning remains probabilistic (that's what makes LLMs useful), while critical constraints are enforced deterministically by the kernel. Deterministic constraints and probabilistic decisions, joined by a feedback loop, yield an architecture that is both flexible and controllable.</p>\n<h2 id=\"core-mechanism-label-propagation\">Core Mechanism: Label Propagation</h2>\n<p>ActPlane's policies are not static allow/deny lists. Instead, they are labeled information-flow policies: processes and files get labels, labels propagate automatically along fork/exec edges and file read/write edges, and rules make decisions based on those labels. The academic roots trace back to <a href=\"https://dl.acm.org/doi/10.1145/3243734.3243776\">CamQuery</a> (CCS 2018) and <a href=\"https://dl.acm.org/doi/10.1145/3127479.3129249\">CamFlow</a> (SoCC 2017), which implemented cross-channel taint propagation and enforcement on an in-kernel provenance graph. ActPlane brings the same idea to the modern eBPF/BPF-LSM substrate, requiring no custom kernel module, and targeting cooperative-but-forgetful AI agents rather than remote adversaries.</p>\n<p>Walking through an example makes this concrete. Say we want to constrain Claude Code's entire process tree. First, declare a label source: <code>source AGENT = exec \"claude\"</code>, meaning whenever a process executes a binary named <code>claude</code>, it gets the <code>AGENT</code> label. Labels aren't generated by default: only behaviors explicitly declared as sources produce them, so the system tracks only the data flows you care about. Similarly, <code>source SCHEMA_CHANGED = file \"src/protocol/**/*.proto\"</code> declares that when these files are written, the writer gets the <code>SCHEMA_CHANGED</code> label.</p>\n<p>Once a label is created, it propagates automatically along two dimensions. Along process lineage: a parent process with the <code>AGENT</code> label forks a child, and the child inherits it. The child execs a new program and inherits it too. The agent starts bash, bash starts Python, Python calls subprocess to start git, and every layer carries the <code>AGENT</code> label. Labels don't get lost at process boundaries. Along file I/O: a process with the <code>AGENT</code> label writes to file <code>f</code>, and <code>f</code> gets the <code>AGENT</code> label too. Later, a process that didn't originally have that label reads <code>f</code> and also gets marked <code>AGENT</code>. When data crosses process boundaries through files, labels follow the data.</p>\n<p>With labels in place, rules are straightforward. <code>kill exec \"git\" \"push\" if AGENT</code> means any process with the <code>AGENT</code> label that tries to execute <code>git push</code> gets terminated. Back to the opening scenario:</p>\n<pre><code class=\"\">Claude Code (AGENT) \n  → bash (inherits AGENT)\n    → python script.py (inherits AGENT) \n      → subprocess: git push (inherits AGENT) ← rule fires here\n</code></pre>\n<p>No matter how deep the nesting or how many process boundaries are crossed, labels propagate along lineage all the way and the rule fires at the final exec call point. The checkpoint has moved from the tool entry down to the kernel's exec call, covering every path the agent might take.</p>\n<p>File-direction label propagation solves a different class of problems. Suppose an agent exports sensitive data from a production database to <code>data.csv</code>: the process gets a <code>DB_SENSITIVE</code> label, and the file does too. A later step reads <code>data.csv</code>, acquiring the <code>DB_SENSITIVE</code> label, then tries to send the content over the network. At that point <code>block connect if DB_SENSITIVE</code> blocks the network connection. The data flow crossed both process and file boundaries, but labels followed it throughout.</p>\n<p>Kernel-level tracking operates at syscall granularity: ActPlane knows process P wrote to file F, but not what content was written. This means over-tainting can occur: a process reads one line from a sensitive file, then writes completely unrelated data to another file, and the second file still gets marked sensitive. In academic literature this is called label creep. In practice three factors mitigate it. Different agent tasks typically run in different process trees, so labels are naturally isolated. Rule matching checks operation-type-plus-label combinations (<code>block connect if DB_SENSITIVE</code> only fires on network connections, not on every file write). And only explicitly declared sources produce labels in the first place. This is a deliberate design tradeoff: better to over-label than to miss real data flows.</p>\n<h2 id=\"temporal-constraints-the-since-clause\">Temporal Constraints: The <code>since</code> Clause</h2>\n<p>Label propagation answers \"who did it\" and \"where did the data come from.\" But many of the constraints mentioned earlier also involve temporal ordering, such as \"run tests before committing,\" \"run protoc after changing specs,\" which require knowing \"has event X happened after event Y.\" ActPlane uses the <code>since</code> clause to reason over the event timeline: \"after X happens and before Y, Z is not allowed.\"</p>\n<p>Combining labels with temporal logic yields highly expressive policy files. The following four rules demonstrate four distinct constraint patterns:</p>\n<figure data-rehype-pretty-code-figure=\"\"><pre tabindex=\"0\" data-language=\"yaml\" data-theme=\"github-dark-dimmed\"><code data-language=\"yaml\" data-theme=\"github-dark-dimmed\" style=\"display: grid;\"><span data-line=\"\"><span style=\"color:#768390\"># actplane.yaml</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">version</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#6CB6FF\">1</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">policy</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#F47067\">|</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  source AGENT = exec \"claude\"</span></span>\n<span data-line=\"\"> </span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  # Track when protocol schema files are modified</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  source SCHEMA_CHANGED = file \"src/protocol/**/*.proto\"</span></span>\n<span data-line=\"\"> </span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  rule no-git-branch:</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    kill exec \"git\" \"branch\"   if AGENT</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    kill exec \"git\" \"worktree\" if AGENT</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    because \"This workspace forbids creating git branches or worktrees.</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">             Use other git commands, or ask the user to manage branches.\"</span></span>\n<span data-line=\"\"> </span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  rule regenerate-after-schema:</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    notify exec \"git\" \"commit\"</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">      if SCHEMA_CHANGED unless after exec \"protoc\" since write \"src/protocol/**\"</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    because \"Protocol schema changed — generated code may be stale.</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">             Run `make proto` to regenerate, then commit.\"</span></span>\n<span data-line=\"\"> </span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  rule test-before-commit:</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    block exec \"git\" \"commit\"</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">      if AGENT unless after exec \"pnpm\" \"test\" since write \"src/**\"</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    because \"Source files changed since last test run.</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">             Run `pnpm test:changed`, then commit.\"</span></span>\n<span data-line=\"\"> </span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">  rule mediate-proddb:</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    block open file \"**/prod.db\"</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">      unless lineage-includes exec \"**/migrate\"</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">    because \"prod.db is reachable only through the migration tool.</span></span>\n<span data-line=\"\"><span style=\"color:#96D0FF\">             Run `./migrate` to access it.\"</span></span></code></pre></figure>\n<p><code>no-git-branch</code> is the simplest: any process in the agent's tree that tries <code>git branch</code> or <code>git worktree</code> is terminated immediately, with no conditions and no temporal logic. The agent receives the <code>because</code> content and knows to use other git commands or ask the user to manage branches.</p>\n<p><code>regenerate-after-schema</code> is a cross-event conditional rule using the notify effect. Its <code>unless</code> clause asks a specific temporal question: since the last time a process wrote to the protocol directory, has <code>protoc</code> been executed? If yes, the commit proceeds; if no, the agent gets a reminder. The key is the <code>since</code> clause's dynamic nature: whenever the protocol directory is written to again, the \"already ran protoc\" state resets and must be re-established, a predicate that dynamically updates on the event timeline rather than a one-time static check.</p>\n<p><code>test-before-commit</code> has similar semantics but is stricter, using the block effect: have tests been run since the last write to <code>src/**</code>? If not, the commit is blocked before it executes. Every source file modification resets the test status, so changing even one line after a passing test run means running them again.</p>\n<p><code>mediate-proddb</code> takes a different approach: instead of relying on labels, it uses <code>lineage-includes</code> to check process ancestry. Any process wanting to open <code>prod.db</code> must have <code>migrate</code> somewhere in its ancestor chain. This rule expresses \"the only legitimate access path\": the agent directly calling <code>open(\"prod.db\")</code> gets blocked, but calling <code>./migrate</code>, which internally opens the file, succeeds. What matters isn't whether the agent holds some abstract permission, but whether its lineage passed through the designated gate program.</p>\n<p>Traditional sandboxes have no concept of \"time\" or \"path\" and only know the current instant's state. By maintaining both an event timeline for temporal reasoning and process lineage for path checking, ActPlane can express workflow constraints and mandatory mediation, not just access control.</p>\n<h2 id=\"agent-integration\">Agent Integration</h2>\n<p>ActPlane delivers rule match reasons to agents through their framework's hook systems, with the kernel remaining the sole authority for observation and enforcement. Hooks only relay match events into the agent's decision context.</p>\n<p>Claude Code integration is configured via <code>.claude/settings.local.json</code> with <code>PostToolUse</code> and <code>PostToolUseFailure</code> hooks:</p>\n<figure data-rehype-pretty-code-figure=\"\"><pre tabindex=\"0\" data-language=\"json\" data-theme=\"github-dark-dimmed\"><code data-language=\"json\" data-theme=\"github-dark-dimmed\" style=\"display: grid;\"><span data-line=\"\"><span style=\"color:#ADBAC7\">{</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">  \"hooks\"</span><span style=\"color:#ADBAC7\">: {</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">    \"PostToolUse\"</span><span style=\"color:#ADBAC7\">: [{ </span><span style=\"color:#8DDB8C\">\"matcher\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"*\"</span><span style=\"color:#ADBAC7\">, </span><span style=\"color:#8DDB8C\">\"hooks\"</span><span style=\"color:#ADBAC7\">: [{ </span><span style=\"color:#8DDB8C\">\"type\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"command\"</span><span style=\"color:#ADBAC7\">, </span><span style=\"color:#8DDB8C\">\"command\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"actplane feedback-hook\"</span><span style=\"color:#ADBAC7\"> }] }],</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">    \"PostToolUseFailure\"</span><span style=\"color:#ADBAC7\">: [{ </span><span style=\"color:#8DDB8C\">\"matcher\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"*\"</span><span style=\"color:#ADBAC7\">, </span><span style=\"color:#8DDB8C\">\"hooks\"</span><span style=\"color:#ADBAC7\">: [{ </span><span style=\"color:#8DDB8C\">\"type\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"command\"</span><span style=\"color:#ADBAC7\">, </span><span style=\"color:#8DDB8C\">\"command\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"actplane feedback-hook\"</span><span style=\"color:#ADBAC7\"> }] }]</span></span>\n<span data-line=\"\"><span style=\"color:#ADBAC7\">  }</span></span>\n<span data-line=\"\"><span style=\"color:#ADBAC7\">}</span></span></code></pre></figure>\n<p>Codex integration via <code>.codex/hooks.json</code>:</p>\n<figure data-rehype-pretty-code-figure=\"\"><pre tabindex=\"0\" data-language=\"json\" data-theme=\"github-dark-dimmed\"><code data-language=\"json\" data-theme=\"github-dark-dimmed\" style=\"display: grid;\"><span data-line=\"\"><span style=\"color:#ADBAC7\">{</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">  \"hooks\"</span><span style=\"color:#ADBAC7\">: {</span></span>\n<span data-line=\"\"><span style=\"color:#8DDB8C\">    \"PostToolUse\"</span><span style=\"color:#ADBAC7\">: [{ </span><span style=\"color:#8DDB8C\">\"matcher\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\".*\"</span><span style=\"color:#ADBAC7\">, </span><span style=\"color:#8DDB8C\">\"hooks\"</span><span style=\"color:#ADBAC7\">: [{ </span><span style=\"color:#8DDB8C\">\"type\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"command\"</span><span style=\"color:#ADBAC7\">, </span><span style=\"color:#8DDB8C\">\"command\"</span><span style=\"color:#ADBAC7\">: </span><span style=\"color:#96D0FF\">\"actplane feedback-hook\"</span><span style=\"color:#ADBAC7\"> }] }]</span></span>\n<span data-line=\"\"><span style=\"color:#ADBAC7\">  }</span></span>\n<span data-line=\"\"><span style=\"color:#ADBAC7\">}</span></span></code></pre></figure>\n<p><code>actplane feedback-hook</code> is straightforward: it checks whether any new rule match events have occurred since its last invocation and, if so, forwards the <code>because</code> content to the hook output. Because the agent runs this hook after every tool call, any triggered rule's reason enters the next decision's context, and the entire integration requires no modifications to the agent framework itself.</p>\n<h2 id=\"runtime-architecture\">Runtime Architecture</h2>\n<p>ActPlane's runtime splits into kernel-space and user-space:</p>\n<pre><code class=\"\">actplane.yaml ─▶ compiler (Rust) ─▶ .rodata config ─▶ eBPF kernel engine\n policy: |        parse + lower DSL    (set_global)      propagate labels,\n                                                          match rules,\n matches ◀─────── ring buffer (in-process, via aya) ◀─── emit on match only\n</code></pre>\n<p>The kernel part (<code>bpf/</code> directory) maintains a per-node label set for processes, files, and network endpoints, executes propagation, evaluates compiled rules, and emits events to user-space via ring buffer only when rules match. Unmatched operations produce zero user-space overhead, which matters because an active agent can trigger hundreds of file operations and process creations per second. If every operation notified user-space for a decision, latency would be unacceptable. Label propagation and rule matching complete entirely in kernel space, and user-space participates only when a rule fires.</p>\n<p>The user-space part is the <code>actplane</code> Rust binary, with eBPF programs precompiled into CO-RE (Compile Once, Run Everywhere) format and embedded in the binary. Installation needs no clang, llvm, libbpf, or any compilation toolchain. The deployment path is <code>cargo install actplane</code> → <code>actplane init</code> to generate a starter config → <code>actplane check</code> to validate rules → <code>sudo actplane run &#x3C;command></code> to execute the agent under the harness. eBPF programs are checked by the kernel verifier, guaranteeing they won't crash the kernel or loop infinitely.</p>\n<p>At runtime the binary loads precompiled eBPF objects in-process via <a href=\"https://github.com/aya-rs/aya\">aya</a>, parses <code>actplane.yaml</code>, compiles the DSL into kernel configuration (written into <code>.rodata</code>), seeds the target process's lineage, and then listens on the ring buffer. Compared with <a href=\"https://tetragon.io/\">Cilium Tetragon</a>, whose <code>matchBinaries</code> + <code>followChildren</code> can propagate a lineage flag along fork/exec (the closest OSS feature to ActPlane's lineage tracking), ActPlane additionally propagates labels across file and network edges and provides semantic feedback to the agent.</p>\n<p>On permissions: <code>actplane run</code> and <code>actplane watch</code> need root or <code>CAP_BPF</code> + <code>CAP_SYS_ADMIN</code> to load the eBPF engine, but once loaded the target command drops back to the current user. <code>actplane check</code> needs no privileges at all, as it only performs static rule validation.</p>\n<h2 id=\"use-cases-and-limitations\">Use Cases and Limitations</h2>\n<p>Kernel-level constraints prove most valuable when multiple agents from different vendors collaborate. When Claude Code calls Codex and Codex calls a custom tool chain, each vendor's framework-level guards only know their own registered tools: Claude Code's hooks don't know Codex's permission config, and vice versa. Framework-level guards assume \"I know what paths the agent will use to operate the system,\" and cross-vendor calls immediately break that assumption. OS-level rules, by contrast, propagate along process lineage regardless of which vendor's runtime is underneath, so a single rule governs the entire cross-vendor execution tree.</p>\n<p>CI/CD environments impose even stricter requirements: agents in build pipelines can't push code, can't modify CI config, and must pass tests before producing build artifacts, all temporal constraints that <code>since</code> clauses are designed to express. In deployments involving sensitive data, agents also need data-flow-level policies like \"data read from prod.db must not flow to the network.\" Traditional sandboxes can't track cross-process data flow at this granularity, but label propagation can.</p>\n<p>ActPlane also has clear boundaries. Because it is built on eBPF, it only runs on Linux 5.8+ with BTF support (<code>/sys/kernel/btf/vmlinux</code>), leaving macOS and Windows agent development scenarios uncovered, though most production deployments are on Linux. Loading eBPF programs requires root or <code>CAP_BPF</code> + <code>CAP_SYS_ADMIN</code>, which some shared servers and cloud containers won't grant. Kernel-level tracking reaches only syscall granularity, so in-process memory operations and encryption/decryption are out of scope. Block mode depends on BPF-LSM, which not all distributions enable by default.</p>\n<h2 id=\"conclusion\">Conclusion</h2>\n<p>Return to the opening scenario: the agent wrote a Python script that called <code>subprocess.run([\"git\", \"push\"])</code>. Under ActPlane, the <code>AGENT</code> label propagates along process lineage from Claude Code to bash to Python to that git three layers deep. The rule fires, the operation is intercepted, and the agent receives a reason and an alternative. What the prompt layer couldn't stop, the kernel layer did.</p>\n<p>An agent's value comes from its flexibility and creativity, but deploying agents requires predictability and safety guarantees. Prompts are ultimately just suggestions, tool-layer guards can be bypassed by a single shell-out, and sandboxes are limited to allow/deny resource isolation. ActPlane adds a layer of deterministic constraints at the kernel, letting agents reason freely while critical operations are adjudicated by information-flow rules. When a constraint triggers, the agent gets actionable feedback, not error codes. ActPlane doesn't replace the first three layers, but closes each of their blind spots.</p>\n<p>In complex systems every single-layer constraint has holes, and agents will naturally find paths through them. Layered enforcement may be a necessary architectural component as agents move toward production deployment.</p>\n<hr>\n<blockquote>\n<p><strong>GitHub</strong>: <a href=\"https://github.com/eunomia-bpf/ActPlane\">github.com/eunomia-bpf/ActPlane</a> — MIT License</p>\n<p>ActPlane is an open-source project from the <a href=\"https://github.com/eunomia-bpf\">eunomia-bpf</a> community. Built on <a href=\"https://github.com/eunomia-bpf/agentsight/\">AgentSight</a>'s eBPF observability foundation.</p>\n</blockquote>","date":"2026-05-31","headings":[{"id":"what-we-actually-want","text":"What We Actually Want","depth":2},{"id":"three-layers-three-blind-spots","text":"Three Layers, Three Blind Spots","depth":2},{"id":"the-path-all-side-effects-must-travel","text":"The Path All Side Effects Must Travel","depth":2},{"id":"harness-not-just-a-sandbox","text":"Harness, Not Just a Sandbox","depth":2},{"id":"core-mechanism-label-propagation","text":"Core Mechanism: Label Propagation","depth":2},{"id":"temporal-constraints-the-since-clause","text":"Temporal Constraints: The since Clause","depth":2},{"id":"agent-integration","text":"Agent Integration","depth":2},{"id":"runtime-architecture","text":"Runtime Architecture","depth":2},{"id":"use-cases-and-limitations","text":"Use Cases and Limitations","depth":2},{"id":"conclusion","text":"Conclusion","depth":2}],"sourcePath":"https://github.com/eunomia-bpf/eunomia.dev/tree/main/docs/blog/posts/actplane.md","metadata":{"updatedAt":"2026-06-14T05:57:17.000Z","createdAt":"2026-06-01T03:03:08.000Z","authors":[{"name":"LinuxDev9002","email":"i@littlefisher.me"},{"name":"云微","email":"1067852565@qq.com"},{"name":"LinuxDev9002","email":"linuxdev8883@example.com"}]},"path":"/blog/2026/05/31/actplane-pushing-agent-harness-enforcement-down-to-kernel-ebpf/","alternates":{"en":"/blog/2026/05/31/actplane-pushing-agent-harness-enforcement-down-to-kernel-ebpf/","zh":"/zh/blog/2026/05/31/actplane-pushing-agent-harness-enforcement-down-to-kernel-ebpf/"},"continuation":{"index":{"title":"Blog","description":"Technical articles on eBPF, bpftime, AI agent observability, GPU tracing, userspace runtimes, and systems research from Eunomia.","href":"/blog/"},"previous":{"title":"Profiling AI Agents with Semantic Flamegraphs","description":"Your AI agent spent $3000 this month. Which activities consumed that budget? agentpprof applies the flamegraph paradigm to AI agent traces, mapping natural language prompts to semantic tags and aggregating them like a CPU profiler. This post explains why existing observability tools fail at budget attribution and how semantic flamegraphs restore aggregation for agent workloads.","href":"/blog/2026/06/24/agentpprof-semantic-flamegraph/"},"next":{"title":"Runtime Observability and Enforcement for Opaque AI Agents with eBPF: Beyond Sandboxes and Approvals","description":"As AI coding agents run autonomously for hours inside harnesses and sandboxes the platform team may not own, approval-based control breaks down. This post argues for separating agent security into three layers (intent authorization, execution isolation, side-effect verification) and using eBPF-based observability (AgentSight) and enforcement (ActPlane) as an independent runtime observability and enforcement below the harness.","href":"/blog/2026/05/25/runtime-security-for-ai-agents/"}},"sidebar":[{"title":"Blog","items":[{"title":"Blog","href":"/blog/","depth":0},{"title":"Profiling AI Agents with Semantic Flamegraphs","href":"/blog/2026/06/24/agentpprof-semantic-flamegraph/","depth":0},{"title":"ActPlane: Pushing Agent Harness Enforcement Down to Kernel eBPF","href":"/blog/2026/05/31/actplane-pushing-agent-harness-enforcement-down-to-kernel-ebpf/","depth":0},{"title":"Runtime Observability and Enforcement for Opaque AI Agents with eBPF: Beyond Sandboxes and Approvals","href":"/blog/2026/05/25/runtime-security-for-ai-agents/","depth":0},{"title":"When CPU Noise Slows Down GPU Inference: Measuring Scheduler and IRQ Impact with eBPF","href":"/blog/2026/05/24/measuring-cpu-noise-in-gpu-inference-with-ebpf/","depth":0},{"title":"ACRFence: Preventing Semantic Rollback Attacks in Agent Checkpoint-Restore","href":"/blog/2026/05/21/acrfence-preventing-semantic-rollback-attacks-in-agent-checkpoint-restore/","depth":0},{"title":"AgentCgroup: What Happens When AI Coding Agents Meet OS Resources?","href":"/blog/2026/02/17/agentcgroup-what-happens-when-ai-coding-agents-meet-os-resources/","depth":0},{"title":"Reverse Engineering Claude Code's SSL Traffic with eBPF","href":"/blog/2026/02/13/reverse-engineering-claude-codes-ssl-traffic-with-ebpf/","depth":0},{"title":"A Taxonomy of GPU Bugs: 19 Defect Classes for CUDA Verification","href":"/blog/2026/01/29/a-taxonomy-of-gpu-bugs-19-defect-classes-for-cuda-verification/","depth":0},{"title":"Architectures for Agent Systems: A Survey of Isolation, Integration, and Governance","href":"/blog/2026/01/11/architectures-for-agent-systems-a-survey-of-isolation-integration-and-governance/","depth":0},{"title":"The GPU Observability Gap: Why We Need eBPF on GPU devices","href":"/blog/2025/10/14/the-gpu-observability-gap-why-we-need-ebpf-on-gpu-devices/","depth":0},{"title":"NVIDIA Open GPU Kernel Modules Comprehensive Source Code Analysis","href":"/blog/2025/10/14/nvidia-open-gpu-kernel-modules-comprehensive-source-code-analysis/","depth":0},{"title":"Understanding iaprof: A Deep Dive into AI/GPU Flame Graph Profiling","href":"/blog/2025/10/11/understanding-iaprof-a-deep-dive-into-ai-gpu-flame-graph-profiling/","depth":0},{"title":"AgentSight: Keeping Your AI Agents Under Control with eBPF-Powered System Observability","href":"/blog/2025/08/26/agentsight-keeping-your-ai-agents-under-control-with-ebpf-powered-system-observability/","depth":0},{"title":"Profiling and Tracing Tools Across System Layers and Architectures","href":"/blog/2025/08/20/profiling-and-tracing-tools-across-system-layers-and-architectures/","depth":0},{"title":"The Modern Memory Testing Arsenal -- A Complete Guide to Benchmarking Tools for Next-Gen Memory Systems","href":"/blog/2025/06/21/the-modern-memory-testing-arsenal-a-complete-guide-to-benchmarking-tools-for-next-gen-memory-systems/","depth":0},{"title":"Observability, Profiling, and Debugging in Systems Conference (2015–2025)","href":"/blog/2025/06/21/observability-profiling-and-debugging-in-systems-conference-2015-2025/","depth":0},{"title":"Checkpoint/Restore Systems: Evolution, Techniques, and Applications in AI Agents","href":"/blog/2025/05/11/checkpoint-restore-systems-evolution-techniques-and-applications-in-ai-agents/","depth":0},{"title":"ASPLOS 2025: Paper Summaries and Insights","href":"/blog/2025/04/25/asplos-2025-paper-summaries-and-insights/","depth":0},{"title":"EuroSys 2025 Paper Summaries and Analysis","href":"/blog/2025/04/25/eurosys-2025-paper-summaries-and-analysis/","depth":0},{"title":"GPU Profiling Under the Hood: An Implementation-Focused Survey of Modern Accelerator Tracing Tools","href":"/blog/2025/04/21/gpu-profiling-under-the-hood-an-implementation-focused-survey-of-modern-accelerator-tracing-tools/","depth":0},{"title":"The Accelerator Toolkit: A Review of Profiling and Tracing for GPUs and other co-processor","href":"/blog/2025/04/11/the-accelerator-toolkit-a-review-of-profiling-and-tracing-for-gpus-and-other-co-processor/","depth":0},{"title":"OS-Level Challenges in LLM Inference and Optimizations","href":"/blog/2025/02/18/os-level-challenges-in-llm-inference-and-optimizations/","depth":0},{"title":"WASI and the WebAssembly Component Model: Current Status","href":"/blog/2025/02/16/wasi-and-the-webassembly-component-model-current-status/","depth":0},{"title":"eBPF Ecosystem Progress in 2024–2025: A Technical Deep Dive","href":"/blog/2025/02/12/ebpf-ecosystem-progress-in-2024-2025-a-technical-deep-dive/","depth":0},{"title":"Security Vulnerabilities Study in Software Extensions and Plugins","href":"/blog/2025/02/10/security-vulnerabilities-study-in-software-extensions-and-plugins/","depth":0},{"title":"Can LLMs understand Linux kernel? A New AI-Powered Approach to Understanding Large Codebases","href":"/blog/2024/10/01/can-llms-understand-linux-kernel-a-new-ai-powered-approach-to-understanding-large-codebases/","depth":0},{"title":"Building High-Performance Userspace eBPF VMs with LLVM","href":"/blog/2024/09/10/building-high-performance-userspace-ebpf-vms-with-llvm/","depth":0},{"title":"The Past, Present, and Future of eBPF and Its Path to Revolutionizing Systems","href":"/blog/2024/08/11/the-past-present-and-future-of-ebpf-and-its-path-to-revolutionizing-systems/","depth":0},{"title":"Simplifying Kernel Programming: The LLM-Powered eBPF Tool","href":"/blog/2024/07/11/simplifying-kernel-programming-the-llm-powered-ebpf-tool/","depth":0},{"title":"The eBPF Evolution and Future: From Linux Origins to Cross-Platform Dominance","href":"/blog/2024/06/11/the-ebpf-evolution-and-future-from-linux-origins-to-cross-platform-dominance/","depth":0},{"title":"Implementing an Inline Hook in C in 5 minutes","href":"/blog/2024/04/11/implementing-an-inline-hook-in-c-in-5-minutes/","depth":0},{"title":"The Evolution and Impact of eBPF: A list of Key Research Papers from Recent Years","href":"/blog/2024/03/11/the-evolution-and-impact-of-ebpf-a-list-of-key-research-papers-from-recent-years/","depth":0},{"title":"Introducing eunomia-bpf v1.0: Simplifying eBPF with CO-RE and WebAssembly","href":"/blog/2024/02/11/introducing-eunomia-bpf-v1-0-simplifying-ebpf-with-co-re-and-webassembly/","depth":0},{"title":"The Secure Path Forward for eBPF runtime: Challenges and Innovations","href":"/blog/2024/02/11/the-secure-path-forward-for-ebpf-runtime-challenges-and-innovations/","depth":0},{"title":"Userspace eBPF Runtimes: Overview and Applications","href":"/blog/2024/01/11/userspace-ebpf-runtimes-overview-and-applications/","depth":0},{"title":"bpftime: Extending eBPF from Kernel to User Space","href":"/blog/2023/11/11/bpftime-extending-ebpf-from-kernel-to-user-space/","depth":0},{"title":"Use ChatGPT to write eBPF programs and trace the Linux kernel with natural language","href":"/blog/2023/09/11/use-chatgpt-to-write-ebpf-programs-and-trace-the-linux-kernel-with-natural-language/","depth":0},{"title":"Simplifying eBPF Development: GitHub Templates and Codespaces for Online Compilation and Execution","href":"/blog/2023/07/11/simplifying-ebpf-development-github-templates-and-codespaces-for-online-compilation-and-execution/","depth":0},{"title":"When Wasm Meets eBPF: Writing, Distributing, Loading, and Running eBPF Programs with WebAssembly","href":"/blog/2023/06/11/when-wasm-meets-ebpf-writing-distributing-loading-and-running-ebpf-programs-with-webassembly/","depth":0},{"title":"eBPF Advanced: Overview of New Kernel Features","href":"/blog/2023/04/11/ebpf-advanced-overview-of-new-kernel-features/","depth":0},{"title":"Progress of eunomia-bpf in March","href":"/blog/2023/03/11/progress-of-eunomia-bpf-in-march/","depth":0},{"title":"eunomia-bpf 0.3.0 Release: Easily Build, Package, and Publish Full eBPF Applications by Writing Kernel-Mode Code","href":"/blog/2023/02/11/eunomia-bpf-0-3-0-release-easily-build-package-and-publish-full-ebpf-applications-by-writing-kernel-mode-code/","depth":0},{"title":"Wasm-bpf: Bridging WebAssembly and eBPF for Kernel Programmability","href":"/blog/2023/02/11/wasm-bpf-bridging-webassembly-and-ebpf-for-kernel-programmability/","depth":0},{"title":"Wasm-bpf: A Common eBPF Kernel Programmability for Cloud-Native Webassembly","href":"/blog/2023/02/11/wasm-bpf-a-common-ebpf-kernel-programmability-for-cloud-native-webassembly/","depth":0},{"title":"eunomia-bpf: Looking forward to 2023, let eBPF sprout wings with Wasm","href":"/blog/2023/01/11/eunomia-bpf-looking-forward-to-2023-let-ebpf-sprout-wings-with-wasm/","depth":0},{"title":"如何在 Linux 显微镜（LMP）项目中开启 eBPF 之旅？","href":"/blog/2022/10/11/如何在-linux-显微镜-lmp-项目中开启-ebpf-之旅/","depth":0},{"title":"Running the ecli on Android 13","href":"/blog/2022/09/11/running-the-ecli-on-android-13/","depth":0},{"title":"在 WebAssembly 中编写 eBPF 程序和使用 libbpf","href":"/blog/2022/02/11/在-webassembly-中编写-ebpf-程序和使用-libbpf/","depth":0},{"title":"在 WebAssembly 中使用 Rust 编写 eBPF 程序并发布 OCI 镜像","href":"/blog/2022/02/11/在-webassembly-中使用-rust-编写-ebpf-程序并发布-oci-镜像/","depth":0}]}]}},"__N_SSG":true}