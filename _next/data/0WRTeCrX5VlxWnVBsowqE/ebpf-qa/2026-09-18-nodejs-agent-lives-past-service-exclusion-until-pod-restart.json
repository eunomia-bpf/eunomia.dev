{"pageProps":{"eyebrow":"eBPF Q&A","page":{"layout":"document","title":"Why does an injected Node.js agent from an eBPF tracer stay inside a process after the service is excluded, until a pod restart?","description":"Short answer: because the injected JS agent is a one-time, process-lifetime attachment that OBI's exclusion rules do not undo.","descriptionIsExcerpt":true,"bodyHtml":"<p><strong>Short answer:</strong> because the injected JS agent is a one-time, process-lifetime attachment that OBI's exclusion rules do not undo. OBI discovers Node.js processes and injects a JS agent into each of them through the Node inspector protocol (<code>SIGUSR1</code> + <code>Runtime.evaluate</code>); the injection is a queued action that runs long after discovery accepted the process, and once the agent is in, it lives for the rest of that process's life. <code>exclude_instrument</code> and friends are <em>discovery-time selection rules</em> — they gate which processes get instrumented, not whether the already-installed agent gets removed. So excluding a service after testing stops OBI from instrumenting <em>new</em> process incarnations, but the pods that are already running still carry the agent (with its per-callback event-loop cost) until those pods restart and a fresh process incarnation is created. That is why a rollout restart is what actually makes the latency drop.</p>\n<h2 id=\"the-two-lifetimes-that-dont-match\">The two lifetimes that don't match</h2>\n<p>The confusion comes from two different objects with different lifetimes:</p>\n<ol>\n<li><strong>The OBI config's exclusion rules</strong> (<code>discovery.exclude_instrument</code>, and per-rule <code>exclude:</code>). These are selection criteria evaluated at discovery time — the same definition format as the inclusion selectors. They answer \"which processes should OBI pick up and instrument?\", and the docs position them for exactly this use: \"avoid instrumenting services typically found in observability environments,\" or excluding a port-forwarder by executable path. Nothing in these rules is a command to processes.</li>\n<li><strong>The injected agent inside the process.</strong> The injector's design comment states the timing directly: \"Injection is queued and runs long after that\" discovery accepted the process, and the target is pinned by PID + start-time (\"a stable reference to the process incarnation\") so that \"the numeric PID alone would let a recycled one be identified, signaled and injected in the original's place.\" Injection is a one-shot event against a <em>process incarnation</em>; it is not a continuously maintained subscription.</li>\n</ol>\n<p>Once the agent is in, its lifetime is the process's lifetime. The <code>fdextractor.js</code> agent even <em>restores</em> the original <code>net</code> prototypes at the top of every re-injection run (so re-injection doesn't double-wrap), and its runtime-metrics cleanup \"stays outside the gate: a re-injection with runtime metrics disabled must tear down what a previous injection installed\" — but nothing in the shipped path removes the agent in response to a config change. The agent does not know OBI excluded the service; it just keeps running the hooks it was installed to run.</p>\n<h2 id=\"why-exclusion-is-a-gate-not-a-retractor\">Why exclusion is a gate, not a retractor</h2>\n<p><code>exclude_instrument</code> follows the same definition format as the inclusion selectors (namespace, labels, executable path, open ports…), and OBI's docs point to it for dropping services it should never instrument — observability stack components, a Docker/socat forwarder that shares a port with the real service. Semantically that is a <em>discovery filter</em>: when OBI (re)scans for processes to instrument, an excluded service is simply not picked. For a service you excluded <em>after</em> it had been instrumented:</p>\n<ul>\n<li>OBI stops picking up new incarnations of it;</li>\n<li>already-running pods keep the agent, with its <code>async_hooks</code> <code>before</code> hook and <code>fs.existsSync</code> sentinel still firing on the event loop;</li>\n<li>a restart (or the pod's natural process lifecycle) creates a new incarnation that discovery now excludes — and only then is the per-callback cost gone.</li>\n</ul>\n<p>So the rollout restart is not a workaround; it is the mechanism by which a discovery-time exclusion becomes an effect on running processes. The eBPF side behaves the same way: the uprobes attach to process incarnations, and an excluded service is no longer attached to when it next starts — the running one is what you have to recycle.</p>\n<h2 id=\"how-to-verify-which-half-you-are-looking-at\">How to verify which half you are looking at</h2>\n<ol>\n<li><strong>Check the config is actually in place before you test anything.</strong> Confirm the <code>exclude_instrument</code> / <code>exclude</code> selectors (or <code>OTEL_EBPF_TARGET_PID</code> overrides) match the service, then verify on a <em>newly started</em> pod that OBI does not instrument it — that is the half the config controls.</li>\n<li><strong>Check the running pods separately.</strong> A pod that was instrumented before the exclusion still carries the agent. The decisive signal is the process's age relative to the config change: anything started before it still pays the per-callback cost, anything started after it does not. A latency comparison that mixes the two will read as \"the exclusion did not work.\"</li>\n<li><strong>Profile the event loop of an excluded-but-already-injected pod.</strong> If p99 still shows the per-callback sentinel cost, the agent is still resident; the fix is to recycle the pod (or, going forward, the agent-uninstall path currently in in-flight work — a closed draft PR that switches the sentinel to <code>fs.existsSync</code> and adds \"uninstall the agent on shutdown,\" which gives the agent an explicit way off a running process rather than relying on process death).</li>\n<li><strong>Do not treat \"still expensive after exclusion\" as a discovery bug.</strong> It is the expected consequence of exclusion being a discovery-time gate. The unresolved boundary is that the shipped flow has no live retraction of an already-installed agent; that is exactly what the in-flight uninstall work is for.</li>\n</ol>\n<h2 id=\"the-limitation-that-decides-it\">The limitation that decides it</h2>\n<p><code>exclude_instrument</code> (and per-rule <code>exclude:</code>) decide which <em>discovery</em> events OBI acts on; they do not send a \"remove yourself\" message to processes that already carry the injected agent. Injection is a one-time, incarnation-scoped event — pinned to a PID + start-time because the PID alone could be recycled — so the agent's removal follows the process's death, not the config's wording. The decisive boundary: <strong>exclusion takes effect at the next process incarnation, not in place.</strong> The practical consequence is that excluding a service mid-testing still requires a rollout restart of the affected pods to realize the latency improvement; the follow-through is to keep exclusion rules in place for the new incarnations and, where the agent-uninstall work lands, use it to stop paying the cost without a restart.</p>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://opentelemetry.io/docs/zero-code/obi/configure/service-discovery/\">OBI docs: Service discovery (<code>exclude_instrument</code> — \"Specify selection criteria for excluding services from being instrumented\", same definition format as the inclusion selectors; <code>exclude:</code> per rule)</a></li>\n<li><a href=\"https://opentelemetry.io/docs/zero-code/obi/troubleshooting/\">OBI docs: Troubleshooting (excluding a port-forwarder from instrumentation by executable path — the exclusion gate in practice)</a></li>\n<li><a href=\"https://raw.githubusercontent.com/open-telemetry/opentelemetry-ebpf-instrumentation/main/pkg/internal/nodejs/injection_target.go\">OBI source: <code>injection_target.go</code> (injection \"is queued and runs long after\" discovery; target pinned by PID + start-time because \"the numeric PID alone would let a recycled one be identified, signaled and injected in the original's place\")</a></li>\n<li><a href=\"https://raw.githubusercontent.com/open-telemetry/opentelemetry-ebpf-instrumentation/main/pkg/internal/nodejs/fdextractor.js\">OBI source: <code>fdextractor.js</code> (agent restores the original <code>net</code> prototypes on every re-injection; runtime-metrics \"Cleanup stays outside the gate: a re-injection with runtime metrics disabled must tear down what a previous injection installed\")</a></li>\n<li><a href=\"https://github.com/open-telemetry/opentelemetry-ebpf-instrumentation/pull/3357\">OBI draft PR #3357: nodejs sentinel performance + \"uninstall the agent on shutdown\" (closed, not merged — the explicit way off a running process that does not exist in the shipped flow)</a></li>\n<li><a href=\"https://nodejs.org/api/async_hooks.html\">Node.js docs: <code>async_hooks</code> (the <code>before</code> hook the injected agent installs; marked experimental, <code>AsyncLocalStorage</code> recommended on performance grounds)</a></li>\n</ul>\n<h2 id=\"community-discussion-today\">Community discussion today</h2>\n<p>The monitored window is a rolling week across two allowlisted Slack archives (both technical); two visible-browser chat workspaces and public mailing-list/forum surfaces were not reviewed this run, and that gap is noted rather than treated as quiet. Several themes recurred from the prior days' windows; the technically new angle is the agent lifecycle.</p>\n<p><strong>Node.js agent lifecycle and exclusion (the question above).</strong> The Node sentinel cost-attribution thread continued with a maintainer-confirmed read: client-span parenting uses the fd-pair map while the per-callback sentinel keeps the trace-context map aligned with the active request, and an additional consumer is external trace/profile correlation — so gating the sentinel only on manual spans + log enrichment could silently break that integration. One user then reported that after excluding a service from tracing they had to roll out a pod restart of all pods to see latency drop, inferring that the tracing process was \"left hanging inside pods even after the exclusion.\" That report is the lifecycle boundary this page answers: the agent is a one-time, incarnation-scoped attachment, so a discovery-time exclusion cannot retract it from a running process — the restart is what removes it. A maintainer's direction (separate per-callback refresh from fd-pair correlation, gate per consumer) and the closed, unmerged draft PR for sentinel performance + uninstall-on-shutdown mean there is no shipped live-retraction path yet; the unresolved boundary is whether OBI will gain an explicit agent-removal trigger.</p>\n<p><strong>OBI config v1→v2 migration and the Helm chart gap (recurring).</strong> A user still hitting \"fields are outside the supported v1-to-v2 migration contract\" on their config; the maintainer's position is unchanged: the all-or-nothing <code>migrate</code> is intentional (a silently-dropped field can produce a valid-looking but materially different config), and the proposed middle ground is an explicit <code>--allow-partial</code> / <code>--best-effort</code> mode that migrates what it can and reports the omitted fields — not a silent default. The related OBI Helm-chart v2 gap (a v1 field still injected into the v2 config schema by the chart's <code>_helpers.tpl</code>) was directed to be filed as a separate issue in the helm-charts repo, including chart/OBI versions, the relevant values, the rendered config, and the validation error.</p>\n<p><strong>Managed agent harnesses and observability lock-in (recurring).</strong> The thread on the hosted Agents API and the general \"vendor holds the telemetry\" pattern: when the agent loop runs in a managed harness, the inner span tree only exists if the harness exports it, and a public beta that exposes no tracing configuration or external trace exporters means generic HTTP-client instrumentation can only reconstruct the outbound crossings. A practitioner proposed metering model/tool calls as an ordinary HTTP receipt header so existing HTTP-client spans capture it without inventing new attribute vocabulary; a GenAI agent span-shape thread (one agent span per turn with model calls and tool executions as siblings, correlated by the tool-call identifier) was answered against the current agent-span conventions.</p>","tags":[],"headings":[{"id":"the-two-lifetimes-that-dont-match","text":"The two lifetimes that don't match","depth":2},{"id":"why-exclusion-is-a-gate-not-a-retractor","text":"Why exclusion is a gate, not a retractor","depth":2},{"id":"how-to-verify-which-half-you-are-looking-at","text":"How to verify which half you are looking at","depth":2},{"id":"the-limitation-that-decides-it","text":"The limitation that decides it","depth":2},{"id":"references","text":"References","depth":2},{"id":"community-discussion-today","text":"Community discussion today","depth":2}],"sourcePath":"https://github.com/eunomia-bpf/eunomia.dev/tree/main/docs/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart.md","metadata":{"updatedAt":"2026-09-19T00:44:31.000Z","createdAt":"2026-09-19T00:44:31.000Z","authors":[{"name":"Yunwei 123","email":"yunwei356@gmail.com"}]},"path":"/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart/","alternates":{"en":"/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart/","zh":"/zh/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart/"},"continuation":{"index":{"title":"eBPF Q&A","description":"Daily, source-grounded answers to recurring questions about eBPF, Linux observability, profiling, runtime extension, and security.","href":"/ebpf-qa/"},"previous":{"title":"Should GenAI agent spans nest each tool execution under the model call that requested it, or keep model calls and tool executions as siblings under one agent span?","description":"Short answer: keep them as siblings. For an in-process agent, one invokeagent span per customer turn with the model calls (chat) and tool executions (executetool) as direct children of that invokeagent is the intended","href":"/ebpf-qa/2026-09-17-genai-agent-invoke-agent-chat-execute-tool-sibling-tree/"},"next":{"title":"Why does the OBI config migration from v1 to v2 refuse to write a file when some fields have no mapping, and what is the middle ground?","description":"Short answer: because obi config migrate is a behavior-preserving conversion, not a translation pass.","href":"/ebpf-qa/2026-09-19-obi-config-migration-v1-v2-partial-fields/"}},"sidebar":[{"title":"eBPF Q&A","items":[{"title":"Can eBPF detect secrets in network traffic without collecting them?","href":"/ebpf-qa/2026-08-08-detect-secrets-with-ebpf/","depth":1},{"title":"Why can an extensible BPF scheduler fail until pahole is upgraded?","href":"/ebpf-qa/2026-08-09-sched-ext-pahole-version/","depth":1},{"title":"Why can inserting a socket into SOCKHASH from TC egress soft-lock the kernel?","href":"/ebpf-qa/2026-08-10-tc-egress-sockhash-soft-lock/","depth":1},{"title":"Why can scxctl accept a scheduler switch while the service still does not start as intended?","href":"/ebpf-qa/2026-08-11-scxctl-scheduler-arguments/","depth":1},{"title":"How should OpenInference coexist with OpenTelemetry's GenAI semantic conventions?","href":"/ebpf-qa/2026-08-13-openinference-opentelemetry-genai/","depth":1},{"title":"How should a Linux-VM eBPF backend support macOS and Windows without mislabeling host coverage?","href":"/ebpf-qa/2026-08-15-cross-platform-ebpf-linux-vm-backend/","depth":1},{"title":"How should eBPF programs carry per-packet metadata across networking hooks?","href":"/ebpf-qa/2026-08-16-cross-hook-packet-metadata/","depth":1},{"title":"Why can classic uprobe BPF programs crash preemptible kernels that use private BPF stacks?","href":"/ebpf-qa/2026-08-17-uprobe-private-stack-preemption-crash/","depth":1},{"title":"Does cgroup v2 cpu.max still limit CPU time under BPF extensible scheduling?","href":"/ebpf-qa/2026-08-18-sched-ext-cgroup-cpu-max/","depth":1},{"title":"Why can libbpf load a BPF object slowly on a host with many kernel modules?","href":"/ebpf-qa/2026-08-19-libbpf-selective-kmod-btf-loading/","depth":1},{"title":"How can you tell whether an OpenTelemetry GenAI attribute is stable enough to depend on?","href":"/ebpf-qa/2026-08-20-opentelemetry-genai-attribute-stability/","depth":1},{"title":"Should an OpenTelemetry metrics producer copy service identity into every data point?","href":"/ebpf-qa/2026-08-21-opentelemetry-resource-attributes-prometheus-labels/","depth":1},{"title":"Why can an unused OBI Prometheus endpoint keep growing memory?","href":"/ebpf-qa/2026-08-22-obi-unused-prometheus-exporter-memory-growth/","depth":1},{"title":"Should an OpenTelemetry GenAI evaluation result carry a verifiable-evidence reference?","href":"/ebpf-qa/2026-08-23-opentelemetry-genai-evaluation-evidence-reference/","depth":1},{"title":"Why can a syscall-rewriting trampoline crash threads created by clone or clone3?","href":"/ebpf-qa/2026-08-24-clone-syscall-trampoline-child-stack/","depth":1},{"title":"How can you start a schedext scheduler at boot without systemd?","href":"/ebpf-qa/2026-08-25-sched-ext-boot-without-systemd/","depth":1},{"title":"Why can a single compare-and-swap lose atomic min/max updates under contention?","href":"/ebpf-qa/2026-08-26-atomic-min-max-cas-contention/","depth":1},{"title":"Can an unprivileged container create its own BPF token?","href":"/ebpf-qa/2026-08-28-unprivileged-container-bpf-token/","depth":1},{"title":"Why must releasing a BPF dynptr invalidate every derived slice and clone?","href":"/ebpf-qa/2026-08-29-bpf-dynptr-release-slice-clone-lifetime/","depth":1},{"title":"Why are PID and TID insufficient to correlate concurrent TLS, HTTP/2, and SSE traffic?","href":"/ebpf-qa/2026-08-31-tls-http2-sse-connection-correlation/","depth":1},{"title":"Why can't an eBPF program read bpftailcall()'s return value?","href":"/ebpf-qa/2026-09-02-bpf-tail-call-return-value/","depth":1},{"title":"Why can reverse-path filtering drop return traffic in an eBPF Kubernetes datapath?","href":"/ebpf-qa/2026-09-03-ebpf-kubernetes-rp-filter-return-traffic/","depth":1},{"title":"Why can the eBPF verifier lose a relationship that is true for the low 32 bits?","href":"/ebpf-qa/2026-09-04-ebpf-verifier-low-32-bit-scalar-equality/","depth":1},{"title":"Which failure scenarios are mandatory before a transient-TCP-recovery eBPF benchmark is meaningful?","href":"/ebpf-qa/2026-09-06-tcp-recovery-benchmark-failure-scenarios/","depth":1},{"title":"Which technical boundaries actually constrain malicious eBPF payloads in practice?","href":"/ebpf-qa/2026-09-08-malicious-ebpf-payload-boundaries/","depth":1},{"title":"What eBPF/kernel limitations should a process behavior reconstruction tool account for in its architecture?","href":"/ebpf-qa/2026-09-10-process-behavior-reconstruction-event-loss/","depth":1},{"title":"Why can't SIGKILL stop a BPF program load after verification has succeeded?","href":"/ebpf-qa/2026-09-11-bpf-post-verification-rewrite-sigkill/","depth":1},{"title":"When should I collect eBPF samples in a BPF ring buffer instead of a perf event array?","href":"/ebpf-qa/2026-09-12-bpf-ringbuffer-vs-perf-event-array/","depth":1},{"title":"Why does a kprobe on a function never fire when the compiler inlined it?","href":"/ebpf-qa/2026-09-13-kprobe-inlined-function-never-fires/","depth":1},{"title":"Why can't a BPF program sleep or block, and what should you use instead in a non-sleepable context?","href":"/ebpf-qa/2026-09-14-bpf-sleep-non-sleepable-context-alternatives/","depth":1},{"title":"Can a BPF program safely read user-space memory that belongs to another process?","href":"/ebpf-qa/2026-09-15-bpf-read-other-task-user-memory-zeros/","depth":1},{"title":"Why did enabling an eBPF tracer on a Node.js service triple p99 on the hot path when the eBPF probes are cheap?","href":"/ebpf-qa/2026-09-16-nodejs-tracer-async-hooks-p99-cost/","depth":1},{"title":"Should GenAI agent spans nest each tool execution under the model call that requested it, or keep model calls and tool executions as siblings under one agent span?","href":"/ebpf-qa/2026-09-17-genai-agent-invoke-agent-chat-execute-tool-sibling-tree/","depth":1},{"title":"Why does an injected Node.js agent from an eBPF tracer stay inside a process after the service is excluded, until a pod restart?","href":"/ebpf-qa/2026-09-18-nodejs-agent-lives-past-service-exclusion-until-pod-restart/","depth":1},{"title":"Why does the OBI config migration from v1 to v2 refuse to write a file when some fields have no mapping, and what is the middle ground?","href":"/ebpf-qa/2026-09-19-obi-config-migration-v1-v2-partial-fields/","depth":1},{"title":"How do you get observability out of a hosted agent harness when the agent loop runs in a vendor service rather than your process?","href":"/ebpf-qa/2026-09-20-hosted-agent-harness-otel-export-boundary/","depth":1},{"title":"eBPF Q&A","href":"/ebpf-qa/","depth":0}]}]}},"__N_SSG":true}