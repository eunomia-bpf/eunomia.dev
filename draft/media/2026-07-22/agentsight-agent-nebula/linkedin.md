# LinkedIn

## Post

A final diff shows what changed. Agent Nebula shows how an AI Agent moved through the repository to get there.

AgentSight's repository evolution replay puts real read, write, create, rename, and delete events back on a timeline. Each point is a file; directory colors show the shape of the codebase, while event ripples reveal where the work is moving. This recording follows ACTplane across multiple Agent sessions.

Generate the same view with `agentsight vis`:
https://github.com/eunomia-bpf/agentsight

#AIAgents #Observability #eBPF

## Publishing Notes

- First visible lines: the final-diff limitation and the missing execution story appear before "see more."
- Primary link: https://github.com/eunomia-bpf/agentsight
- Media: `agentvis/examples/actplane-agent-nebula.gif` from the AgentSight repository.
- Alt text: Animated repository map of ACTplane. Each point represents a file, colors group top-level directories, and ripples show file reads, writes, creates, renames, and deletes across recorded AI Agent sessions.
- Visibility target: Public.
- QA state: Copy and source media prepared locally; composer preview, upload rendering, crop, account, and visibility still need browser QA before publishing.
