# 掘金发布稿：39-nginx 教程

- 状态：待提交（准备于 2026-09-24，目标 LA 2026-09-25 正常额度；09-24 的正常额度与补发额度均已用尽，下一干净掘金正常额度为 LA 2026-09-25）
- 正文：`juejin-body.md`（源文 `docs/tutorials/39-nginx/README.zh.md` 移除源 H1，其余正文逐字保留；4838 字符、7904 字节、6 个 H2、5 个 H3、0 个 H4、3 个代码块 [2 bt、1 console]、0 张图片、0 个表格、4 条唯一外链、无相对链接）
- 标题：使用 eBPF 跟踪 Nginx 请求（源 H1 逐字）
- 原文：https://eunomia.dev/zh/tutorials/39-nginx/
- 代码：https://github.com/eunomia-bpf/bpf-developer-tutorial/tree/main/src/39-nginx
- 分类：后端
- 标签：`Linux`、`后端`、`性能优化`
- 图片：无正文图片
- 外链（4 条，保持源文）：bpftime、bpf-developer-tutorial、33-funclatency 源码、eunomia.dev/tutorials/
- 已核查：`platforms/juejin.json` 无 39-nginx 条目，源文未被掘金发布过，可首发布
- 提交注意：分类 chip 需真实 CDP `page.mouse` 指针序列；标签用原生 setter（先清空残留）加冒泡 `input` 事件，再按 `getBoundingClientRect().width > 0` 选出可见 `.byte-select-option` 并真实点击；「确定并发布」需真实指针序列，勿用合成 `click()`
- 已预置草稿：编辑器中草稿 id `7689029864910929970`（标题、4838 字符正文、分类 `后端`、标签 `Linux`/`后端`/`性能优化` 全部已服务端持久化，重载后回读一致）。到点只需打开该草稿并提交，无需重建内容。
- 提交结果判定：以 `/post/<id>` 的 HTTP 状态为准。返回 200 且无 `审核中`/`文章有更新` 标记即记 `confirmed`；仅 `/spost/<id>` 渲染且 `/post/<id>` 404 时记 `review_pending`
- 恢复条件：未确认公开前不得重复提交同一源文
